---
title: AI 提示词与协作方法
tags: [ai, prompt, 方法论, vibecoding]
---

# AI 提示词与协作方法

## 一、最好的 vibecoding 提示词
不是「帮我写一个 app」，而是：
> 我想做一个 XXX 项目，先别着急写代码，去 github 上找几个类似的开源项目，比较它们的功能、架构、技术栈和优缺点，再给我一份实现方案，等我确定后再动手。

**先找轮子，再定方案，最后开工。** AI 写代码已经很快了，真正拉开差距的是有没有让它先研究别人踩过的坑，避免重复造轮子。

## 二、描述技术栈的方式
与 AI 讨论需要使用的技术栈，例如：
> 基于 wpf、.net framework 4.0，编写一个串口调试工具，需要包含串口选择、波特率、数据位、停止位，包含打开/关闭串口按钮，显示串口的收发记录（包含16进制内容、文本编码内容），发送16进制内容或文本，且可选择文本编码（参考什么软件来画，描述清楚对应功能）

## 三、让 AI 维护旧代码的「贼牛方法」
以前写过的代码全部交给 agent 维护，就可以解放自己做新东西了。

**工具链**：`keil + vscode + git + claude + codex + claude design`

**流程**：
1. VSCode 下载 keil 插件和 claude code 插件
2. 用 VSCode 打开以前产品项目的代码，让 claude code 阅读你写的代码，总结生成 README（含整个工程结构、功能、日志）
3. 用 codex 打开这个工程，阅读 claude 写的日志，再 review 整个代码，提出有问题的地方，总结下来
4. 交给 claude code debug，更新日志，测试没问题就提交仓库
5. 做新东西也可以这么操作，**但先实现整体功能和框架，再给 AI 做**，不然表述的语言很难规范 AI 的做法

## 四、让 AI 自我调试
- 让 Claude 给出自我调试方案，问它少什么，然后搭建自动化验证的环境，丢数据手册和说明文档，让它自我调试
- 与 AI 聊架构，确定架构和函数分配（哪个函数负责什么），再让 AI 写具体实现

## 五、VSCode + 代码书写
- 写代码装 VSCode，找到 codex 扩展，装本地代理和 ccswitch，申请 deepseek v4 api
- 现在可以让 codex 写代码，然后直接编译写入设备，还可以对串口监控，有问题复制代码丢到 codex 里面现场解决，完美闭环

## 六、model 相关工具
- `codex vs codex workbuddy`：硬件目前 codex 可以接入嘉立创直接帮忙画，若想解决线路问题国内的 deepseek、workbuddy 也可以，直接给文件就行
- LLM：gemini cli
