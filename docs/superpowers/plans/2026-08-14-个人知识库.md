# 个人知识库 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `obsidian_test` 内建一个可迁移、可云端的多领域卡片盒知识库(纯 Markdown + git + 相对路径图片)。

**Architecture:** 顶层按领域分目录(技术/工作/生活/学习,各含 inbox/卡片/文献/MOC),加一个时间流「记录区」(日志/周报/收支/单词)。附件统一放 `_assets/` 用相对路径引用;7 个模板;5 个第三方插件;git 独立成库。

**Tech Stack:** Obsidian(Markdown + frontmatter + wikilink)、git、Obsidian 核心插件(模板/Daily Notes/关系图谱)、5 个社区插件(Obsidian Git / Templater / Dataview / Calendar / Paste image rename)。

**Spec:** `docs/superpowers/specs/2026-08-14-个人知识库-design.md`

## Global Constraints

- 库根 = `C:\Users\Administrator\Desktop\agent_skill\obsidian_test`(即 `obsidian_test/`)。
- `git init` 独立成库,不混入父仓库 `agent_skill`。
- 附件相对路径配置:`attachmentFolderPath: "_assets"`。
- `.gitignore` 排除:`.obsidian/workspace*.json`、`.trash/`、`.DS_Store`。
- 领域目录共 4 个:技术/工作/生活/学习,各含 `inbox/`、`卡片/`、`文献/`、`MOC/`。
- 记录区 4 类:日志/周报/收支/单词,时间流组织,不套卡片盒 ID 命名。
- 卡片盒文件名 = 时间戳 ID(如 `202608141130.md`),frontmatter 含 `title:` 与 `aliases: [标题]`。
- 模板 7 个:卡片/文献笔记/MOC/日志/周报/收支/单词;动态字段用 Templater 语法。
- 第三方插件仅 5 个:Obsidian Git、Templater、Dataview、Calendar、Paste image rename。
- 不装其他第三方插件;领域内不再细分;不设其他固定记录区。

---

### Task 1: 建目录结构 + git 独立成库

**Files:**
- Create: `技术/inbox/`, `技术/卡片/`, `技术/文献/`, `技术/MOC/`(各含占位 `.gitkeep`)
- Create: `工作/inbox/`, `工作/卡片/`, `工作/文献/`, `工作/MOC/`
- Create: `生活/inbox/`, `生活/卡片/`, `生活/文献/`, `生活/MOC/`
- Create: `学习/inbox/`, `学习/卡片/`, `学习/文献/`, `学习/MOC/`
- Create: `记录/日志/`, `记录/周报/`, `记录/收支/`, `记录/单词/`
- Create: `_assets/`
- Create: `.gitignore`

**Interfaces:**
- Consumes: 无(首个任务)。
- Produces: 目录树(后续任务在其中放文件);`.gitignore` 规则(排除 `.obsidian/workspace*.json`、`.trash/`、`.DS_Store`)。

- [ ] **Step 1: 生成目录树(空目录用 `.gitkeep` 占位)**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
mkdir -p 技术/{inbox,卡片,文献,MOC} 工作/{inbox,卡片,文献,MOC} 生活/{inbox,卡片,文献,MOC} 学习/{inbox,卡片,文献,MOC} 记录/{日志,周报,收支,单词} _assets
for d in 技术 工作 生活 学习; do
  touch "$d/inbox/.gitkeep" "$d/卡片/.gitkeep" "$d/文献/.gitkeep" "$d/MOC/.gitkeep"
done
touch 记录/日志/.gitkeep 记录/周报/.gitkeep 记录/收支/.gitkeep 记录/单词/.gitkeep
touch _assets/.gitkeep
```

- [ ] **Step 2: 写 `.gitignore`**

写入 `C:\Users\Administrator\Desktop\agent_skill\obsidian_test\.gitignore`:

```gitignore
.obsidian/workspace*.json
.trash/
.DS_Store
```

- [ ] **Step 3: 校验目录存在**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
test -f 技术/inbox/.gitkeep && test -f 学习/MOC/.gitkeep && test -f 记录/单词/.gitkeep && test -f _assets/.gitkeep && echo PASS
```
Expected: `PASS`

- [ ] **Step 4: git init + 提交**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
git init -q   # 若已在 Task 1 前由 spec 流程 init 过,则跳过本行
git add .gitignore 技术 工作 生活 学习 记录 _assets
git commit -q -m "feat: 目录结构 + .gitignore + 独立 git 库"
```

注意:spec 阶段已 `git init` 并提交过 `docs/`,本任务不要重复 init;`git add` 只加本任务的文件,不要把 `docs/handoff/`(未跟踪)误加。

---

### Task 2: `.obsidian` 配置(附件相对路径 + 核心插件)

**Files:**
- Create: `.obsidian/app.json`
- Create: `.obsidian/core-plugins.json`

**Interfaces:**
- Consumes: 目录 `_assets/`(Task 1)。
- Produces: `attachmentFolderPath` 相对路径配置;核心插件启用状态(Daily Notes、模板、关系图谱等后续任务依赖)。

- [ ] **Step 1: 写 `.obsidian/app.json`**

写入 `C:\Users\Administrator\Desktop\agent_skill\obsidian_test\.obsidian\app.json`:

```json
{
  "attachmentFolderPath": "_assets",
  "newLinkFormat": "relative",
  "useMarkdownLinks": false,
  "alwaysUpdateLinks": true
}
```

- [ ] **Step 2: 写 `.obsidian/core-plugins.json`**

写入 `C:\Users\Administrator\Desktop\agent_skill\obsidian_test\.obsidian\core-plugins.json`:

```json
[
  "file-explorer",
  "global-search",
  "switcher",
  "graph",
  "backlink",
  "outgoing-link",
  "tag-pane",
  "page-preview",
  "daily-notes",
  "templates",
  "note-composer",
  "command-palette",
  "editor-status",
  "outline",
  "word-count",
  "file-recovery"
]
```

- [ ] **Step 3: 校验配置内容**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
grep -q '"_assets"' .obsidian/app.json && grep -q '"graph"' .obsidian/core-plugins.json && grep -q '"daily-notes"' .obsidian/core-plugins.json && echo PASS
```
Expected: `PASS`

- [ ] **Step 4: 提交**

```bash
git add .obsidian/app.json .obsidian/core-plugins.json
git commit -q -m "feat: .obsidian 附件相对路径 + 核心插件配置"
```

---

### Task 3: 社区插件启用配置

**Files:**
- Create: `.obsidian/community-plugins.json`

**Interfaces:**
- Consumes: 无(独立配置)。
- Produces: 5 个社区插件的启用清单(Obsidian 识别后启用;插件本体由用户在 Obsidian 内安装)。

- [ ] **Step 1: 写 `.obsidian/community-plugins.json`**

写入 `C:\Users\Administrator\Desktop\agent_skill\obsidian_test\.obsidian\community-plugins.json`:

```json
[
  "obsidian-git",
  "templater-obsidian",
  "dataview",
  "calendar",
  "obsidian-paste-image-rename"
]
```

- [ ] **Step 2: 校验 5 个 ID 存在**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
for id in obsidian-git templater-obsidian dataview calendar obsidian-paste-image-rename; do
  grep -q "$id" .obsidian/community-plugins.json || { echo "MISSING $id"; exit 1; }
done && echo PASS
```
Expected: `PASS`

- [ ] **Step 3: 提交**

```bash
git add .obsidian/community-plugins.json
git commit -q -m "feat: 启用 5 个社区插件"
```

---

### Task 4: 7 个模板

**Files:**
- Create: `模板/卡片.md`
- Create: `模板/文献笔记.md`
- Create: `模板/MOC.md`
- Create: `模板/日志.md`
- Create: `模板/周报.md`
- Create: `模板/收支.md`
- Create: `模板/单词.md`

**Interfaces:**
- Consumes: Templater 插件(Task 3 已启用)解析动态字段。
- Produces: 7 个模板文件,供 Obsidian 模板核心插件 / Templater 调用。

- [ ] **Step 1: 写 `模板/卡片.md`(永久笔记)**

```markdown
---
title: 
aliases: 
tags: []
created: <% tp.date.now("YYYY-MM-DD") %>
---

# <% tp.file.cursor(1) %>
```

- [ ] **Step 2: 写 `模板/文献笔记.md`**

```markdown
---
title: 
aliases: 
tags: [文献]
source: 
author: 
created: <% tp.date.now("YYYY-MM-DD") %>
---

# <% tp.file.cursor(1) %>

## 摘要

## 笔记

## 关联
```

- [ ] **Step 3: 写 `模板/MOC.md`**

```markdown
---
title: 
aliases: 
tags: [MOC]
created: <% tp.date.now("YYYY-MM-DD") %>
---

# <% tp.file.cursor(1) %>

## 概述

## 条目
- 
```

- [ ] **Step 4: 写 `模板/日志.md`**

```markdown
---
date: <% tp.date.now("YYYY-MM-DD") %>
---

# <% tp.date.now("YYYY-MM-DD") %>

## 今日
```

- [ ] **Step 5: 写 `模板/周报.md`**

```markdown
---
week: <% tp.date.now("YYYY-[W]ww") %>
---

# 周报 <% tp.date.now("YYYY-[W]ww") %>

## 本周完成

## 问题与风险

## 下周计划
```

- [ ] **Step 6: 写 `模板/收支.md`**

```markdown
---
date: <% tp.date.now("YYYY-MM-DD") %>
type: 支出
amount: 
category: 
---

# <% tp.file.cursor(1) %>

- 日期: <% tp.date.now("YYYY-MM-DD") %>
- 金额: 
- 类别: 
```

- [ ] **Step 7: 写 `模板/单词.md`**

```markdown
---
word: 
phonetic: 
meaning: 
created: <% tp.date.now("YYYY-MM-DD") %>
---

# <% tp.file.cursor(1) %>

- 音标: 
- 释义: 
- 例句: 
```

- [ ] **Step 8: 校验 7 个模板存在且含 Templater 语法**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
for f in 模板/卡片.md 模板/文献笔记.md 模板/MOC.md 模板/日志.md 模板/周报.md 模板/收支.md 模板/单词.md; do
  test -f "$f" || { echo "MISSING $f"; exit 1; }
done
grep -q 'tp\.' 模板/卡片.md && echo PASS
```
Expected: `PASS`

- [ ] **Step 9: 提交**

```bash
git add 模板
git commit -q -m "feat: 7 个笔记模板(Templater 语法)"
```

---

### Task 5: README + 最终校验 + 初始提交

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 全部前序任务产物(目录、`.obsidian` 配置、模板)。
- Produces: 用法说明与换电脑恢复流程文档;库的最终可用状态。

- [ ] **Step 1: 写 `README.md`**

写入 `C:\Users\Administrator\Desktop\agent_skill\obsidian_test\README.md`:

````markdown
# 个人知识库

多领域卡片盒(Zettelkasten)个人知识库,纯 Markdown + git 可迁移。

## 结构

- 领域目录:`技术/`、`工作/`、`生活/`、`学习/`,各含 `inbox/ 卡片/ 文献/ MOC/`。
- 记录区:`记录/` 下 `日志/ 周报/ 收支/ 单词/`,时间流组织。
- 附件:`_assets/`,相对路径引用。
- 模板:`模板/`,7 个。

## 用法

1. 用 Obsidian 打开本目录(作为 vault)。
2. 新建卡片:用模板 `卡片`,标题写主题名,aliases 同填主题名。
3. 链接用 `[[标题]]`(靠 aliases 解析到时间戳 ID 文件名)。
4. 日志由 Daily Notes 自动建;周报/收支/单词用对应模板。

## 插件(5 个,需在 Obsidian 内安装)

Obsidian Git、Templater、Dataview、Calendar、Paste image rename。

## 换电脑恢复

```bash
git clone <remote-url> my-vault
```
然后用 Obsidian 打开 `my-vault` 目录即可;附件为相对路径,自动生效。

## 同步

用 Obsidian Git 插件自动 commit/push/pull,或手动 `git add -A && git commit && git push`。
````

- [ ] **Step 2: 全库结构校验**

```bash
cd "C:\Users\Administrator\Desktop\agent_skill\obsidian_test"
test -f .obsidian/app.json && test -f .obsidian/community-plugins.json && test -f README.md && test -d _assets && test -f 模板/卡片.md && echo PASS
```
Expected: `PASS`

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -q -m "docs: README 用法 + 恢复流程"
```

---

## 完成标准

- `git log` 含 5 个提交:目录 / `.obsidian` / 插件 / 模板 / README。
- `obsidian_test` 是独立 git 库(非父仓库子模块/子目录暂存)。
- 用 Obsidian 打开 `obsidian_test` 后:领域目录与记录区可见、7 个模板可用、附件落到 `_assets/`、5 插件待装。
