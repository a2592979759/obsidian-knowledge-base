# 代码随想录

## 动态规划

### 0/1背包问题

---

#### 1、01背包问题

``` c
二维dp数组的含义:dp[i][j]表示下标[0,i]的物品任取放进容量为j的背包里的最大价值
递推公式:dp[i][j]
    不放物品i的最大价值:dp[i-1][j];
    放物品i的最大价值:dp[i-1][j-weight[i]] + value[i];
dp[i][j] = max(dp[i-1][j],dp[i-1][j-weight[i]] + value[i])
一维dp数组的含义:dp[j]表示容量为j的背包所能装的最大价值
递推公式:dp[j]
    不放物品i的最大价值:dp[j];
    放物品i的最大价值:dp[j - weight[i]] + value[i];
	dp[j] = max(dp[j],dp[j - weight[i]] + value[i]);
```



#### 2、分割等和子集

#### 3、目标和

#### 4、一和零

