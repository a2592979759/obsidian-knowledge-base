

## SPI学习

### spi协议



 

### spi的四种时序



 

### adc128s102的spi时序图(官方手册)

CPOL = 1,CPHA = 1



DIN(对于adc芯片而言是输入) = MOSI

ADDR代表选择哪个通道



### zynq的spi外设控制器(了解)





### zynq控制器传输模式





### zynq的spi外设寄存器(使用裸机外设库进行编程,基于此些寄存器(对应于ug585)可以做了解和参考)





### zynq的spi处理设计框图分析







### SPI代码处理

~~~c
通过官方的芯片数据手册分析出spi的模式，然后在soc的外设寄存器配置对应的spi模式(其余也有可能可以读到数据)
spi是先处理高bit
依据时隙进行处理，明确处理与地址对应的大小端关系。(既地址高字节先发送还是地址低字节先发送)
首先定义数组datRcv[2] 因为spi的时序是每次发送16bit数据
在这又要明确datRcv的0对应spi发送的高字节还是低字节（使用库则需要研究其先处理高字节还是低字节）
发送明确AADR在先处理字节的多少位,进行移位赋值即可
接收明确实际数据在多少位,做屏蔽和掩码即可
~~~



### ILA(嵌入式逻辑分析仪),相当于示波器的作用

需要一个单独的时钟，所以在下面给一个与spi时钟对应的FCLK_CLK1(时钟频率要高于被抓取的信号)







将ILA例化到顶层文件中即可



### 74HC595是特殊的spi时序

数码管显示(存在位选和片选)

其只在上升沿进行采样，而不关心初始电平



数据如何译码？(既发送的数据其如何解析)



对应于zynq控制器传输模式产生脉冲的形式









修改zynq的xdc文件(注意target)



### c代码数码管的数据定义







可以查看数据手册大体查看74HC595的频率





如果需要进行优化，则需要理解库函数对应的功能.然后直接控制寄存器的方式进行处理即可。



