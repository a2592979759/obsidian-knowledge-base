---
title: 嵌入式软件技能开发与架构积累
tags: [嵌入式, skill, 架构, 积累]
---

# 嵌入式软件技能开发与架构积累

## 一、开发想法（agent_skill_think）
- **确定范围**：搞清楚工具用在哪，在 GitHub 找开源项目拷贝到本地
- **保持聊天内容可复用**：细节学习 Claude 操作，达到多次复用
- **先跑通再迭代**：把功能近似项目 pull 到本地，先跑通代码，再设计总框图。先调一个近乎完美的 Qt 上位机出来，要有可扩展性，尽量多用 Qt 接口，同时熟悉 C++，之后需求都在此项目叠加，做好版本管理
- **形成自己的 skill/mcp**：先问它要并生成一个，再在其基础上完善。这样自己的开发流程确定，带目的去积累才能有效成长
- **采用总分结构**：分几个 skill 和 mcp，总分形式逐步完善。有现成参考先跑通再按需求走，一定要有基础版本迭代，不要直接开写。看官网 demo、git、博客有没有现成的，先跑通再完善，作为技术可行性验证
- **三种工作方式**：结合 claude、vscode、git，明确三种工作方式，用总分结构固定做一件事情的顺序。后续尝试 kimi3.0，skill 成熟即可
- 记录方式：想到什么就写，后面梳理，然后立马去做，不要拖延

## 二、嵌入式代码积累（AI 定位）
- **驱动开发**：驱动不会写也没关系，能看懂能理解就行，把芯片手册、需求和样板代码丢给 GPT 就能出来。大公司有单独驱动岗但要高学历
- **结论**：主要做应用开发，驱动=取硬件厂商（其余做适配），有问题找 FAE
- 软件架构：**与 AI 聊架构，确定架构和函数分配（哪个函数负责什么），让 AI 写具体实现函数**，一般 AI 具体函数实现不会出大问题
- 芯片：工作很少用 STM32，用 ITE、新塘、RTK、GD（对照 ST），很多与 ST pin-to-pin，甚至 ST 库可直接运行（可能有小问题）

## 三、代码架构 skill
- 已有一个命名和规范 skill（`embedded-c-naming`），接下来考虑嵌入式写代码架构层面的 skill，约束行为，让它知道怎么处理
- 预期结合 3-5 个 skill 逐步完善嵌入式软件**开发流程**，写出来的代码可复用性高，用对应关键词触发
- 积累专属命名池
- `embedded-soft-skills` 总入口，路由到：命名规范（embedded-c-naming）、分层架构（driver-layer-architecture）、MCU 软件架构（mcu-soft-architecture）

### 想蒸馏的架构 skill
- 蒸馏出一整套**代码架构 skill**：参考 `SPI_ST7789`（飞书上面的架构）与 Linux 的 platform 架构设计
- 代码架构 skill 同时结合代码规范 skill 合并
- 参考 `嵌入式软件架构设计.vsdx` 添加初步架构相关的 visio 图
- 驱动那块不用动，业务相当于适配层（adaptation 层）的操作，具体怎么分层还需要用多项目打磨
- 描述完需求得到 spec 后，让 skill 参考选择最好的架构

## 四、积累的方法论
- 每学会的技能都要记录，方便复习，包括使用 AI 的记录
- 形成自己的系统方法论，而不是让知识总是零散的学习
- 用 `+`、`-`、`*` 等符号做前缀标记（临时想法 → 梳理）
- superpower 的流程值得学习借鉴——这是一整套完整的方法论
- 很多东西就差临门一脚了

## 五、已有 plugin / skill / mcp 记录
- **plugin**：superpowers、ponytail、handoff、caveman、mattpocock、grillme、其他 plugin and mcp server
- **skill**：embedded-c-naming、embedded-soft-skills
- 相关插件：`grill me`（无限拷问，结合 trellis 做文档和记忆）
- superpowers 各插件有一个可以直接用来创建 skill；handoff 可以恢复保存的会话框；ponytail 据说可与 superpowers 共用（未验证）
