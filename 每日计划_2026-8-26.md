---
tags:
  - 计划
date: 2026-08-26
---
![[每日代办事项]]
- [ ] 练习使用obsidian进行个人知识库的搭建
- [ ] 使用claude进行obsidian的markdown格式编写.md文件
- [ ] 继续整理embedded-soft-skills并考虑整理embedded-hardware-skills
- [ ] 学习obsidian与ai相关的专业知识并做归纳

## 整理embedded-soft-skills

``询问ai：使用embedded-soft-skills搭建一个前后台+状态机的框架

# 前后台 + 状态机 框架

> 基于 `embedded-soft-skills: mcu-soft-architecture` 落地的一套裸机业务架构骨架。
> 核心思想（vsdx）：**中断只记录「发生了什么」，主循环决定「该怎么切状态」。**

## 分层全景

```
┌────────────────────────────────────────────────────┐
│  App 应用层     app_fsm / key_fsm / app_run          │
│                 业务任务、状态机、事件分发            │
├────────────────────────────────────────────────────┤
│  Middleware     sched 前后台调度器                    │
│                 evt_bus 同步订阅发布（模块解耦）      │
│                 evtq 异步事件队列（ISR→主循环）       │
├────────────────────────────────────────────────────┤
│  Core/Driver    bsp_systick 硬件节拍（唯一移植点）    │
└────────────────────────────────────────────────────┘
```

## 目录

```
状态机、前后台/
├── README.md
├── main.c                  启动 + 主循环调度入口
├── Core/
│   ├── bsp_systick.h/.c    1ms 硬件节拍抽象（移植点）
├── Middleware/
│   ├── fw_types.h            形参位宽类型别名（u8/u32，仅用于形参）
│   ├── sched.h/.c            前后台调度器（定时器模拟任务）
│   ├── evt_bus.h/.c          事件总线（同步 订阅-发布）
│   └── evtq.h/.c             事件队列（异步 SPSC 环形缓冲）
└── App/
    ├── key_fsm.h/.c        按键消抖状态机（模块 FSM）
    ├── app_fsm.h/.c        产品模式状态机（APP FSM）
    └── app_run.h/.c        业务任务注册 + 事件分发
```

## 事件链（本框架的核心）

```
①硬件中断（按键/串口/故障）    ② 1ms 节拍 ISR
        │  evtq_post() 入队             │ sched_tick()
        │（ISR 安全，不做事）            │ 递减 remaining，到期置 ready
        ▼                              ▼
   evtq 异步事件队列  ─────────→  主循环 sched_run()
        │                                    │ 轮询 ready 执行任务
        │ app_dispatch_task 出队             ▼
        ▼                              周期任务（如 10ms 扫键）
   app_run_dispatch()  → 查 dst/id  →  key_fsm / app_fsm
                                         │ 调用 Driver 接口
                                         ▼
                                    完成动作 / 切状态
```

## 三个状态与三种事件（vsdx）

**状态机五要素**：状态、事件、动作、转移、当前状态。`当前状态 + 事件 -> 动作 + 下一个状态`。

**事件三来源**：
1. 主循环轮询（读按键/传感器，生成事件）
2. 中断置标志/入队（volatile 标志或 evtq_post）
3. 定时器计时（超时生成 timeout 事件）

## 怎么加一个新任务

```c
/* app_run.c */
static void app_my_task(void);           /* 任务回调 */

void app_run_init(void)
{
    sched_add_task(10, app_my_task);   /* 每 10ms 执行一次 */
}

static void app_my_task(void)
{
    /* ... */
}
```

## 怎么加一个新状态机

```c
/* my_fsm.h */
typedef enum { eMy_state_idle = 0, eMy_state_run, eMy_state_done } eMy_state_t;

void my_fsm_init(void);
void my_fsm_handle_event(uint32_t ueEvent);
```
```c
/* my_fsm.c */
static eMy_state_t s_e_current = eMy_state_idle;

void my_fsm_handle_event(uint32_t ueEvent)
{
    switch (s_e_current)
    {
    case eMy_state_idle:
        if (ueEvent == eMy_event_start) { s_e_current = eMy_state_run; }
        break;
        /* ... */
    }
}
```

## 怎么加一个新事件

1. `evtq.h` 的 `eEvt` 枚举里加事件号
2. 中断里 `evtq_post(&evt)`（`evt.dst` 填目标模块）
3. `app_run.c` 的 `app_dispatch()` 里加 `case` 转给对应 `xxx_fsm_handle_event()`

## 移植（换芯片/换定时器）

只需改 `Core/bsp_systick.c`：把芯片的 1ms 定时器初始化写好，并在它的中断里调用 `sched_tick()`。其余框架层与芯片无关。

## 审查要点（对应 skill Checklist）

- 中断是否只入队/置标志，状态切换都在主循环/任务？（是：evtq_post / sched_tick）
- 每个模块是否独立一个 FSM，而非一个全局大状态机？（key_fsm + app_fsm 分离）
- 环形队列是否 SPSC 无锁、head/tail 是否 volatile？
- 任务 WCET 是否小于节拍周期？高优先级任务放任务表前面。

### ponytail 备注
- 框架未内置 Driver 具体硬件实现：`bsp_systick` 是唯一移植点，其余保持接口纯净。
- `evt_bus`（同步）与 `evtq`（异步）并存：中断里只能用 `evtq_post`，绝不在 ISR 跑 `evt_bus_publish` 的回调。
