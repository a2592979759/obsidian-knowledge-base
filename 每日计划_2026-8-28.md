---
date: 2026-08-28
tags:
  - 计划
  - 学习计划
  - 每日任务
author: lqq
---
![[每日代办事项]]


- [x] 练习使用obsidian进行个人知识库的搭建
- [x] 使用claude进行obsidian的markdown格式编写.md文件
- [x] 继续整理embedded-soft-skills并考虑整理embedded-hardware-skills
- [ ] 视频学习obsidian与ai相关的专业知识并做归纳
- [x] 留意到有些视频讲开源项目可以协助obisidian将pdf转化并提纯为md
- [x] 优化embedded-hardware-skills中的mcu-soft-architecture
- [ ] python3.12安装、pdf转md安装
- [ ] 杂项


## 应用逻辑架构
---
name: mcu-soft-architecture
description: 当设计嵌入式 MCU 软件架构时需要确定应用层的组织方式时使用。触发于"状态机"、"FSM"、"事件驱动"、"发布订阅"、"pub/sub"、"生产者消费者"、"环形缓冲区"、"前后台"、"定时器调度"、"软定时器"、"FreeRTOS"、"任务划分"、"RTOS架构"、"事件总线"、"EventBus" 等短语，或涉及裸机业务架构（状态机/事件驱动/任务调度）与 FreeRTOS 软件架构的代码组织时。
---

# MCU 软件架构（裸机业务架构 + FreeRTOS 架构）

为嵌入式 MCU **应用层/中间件层** 定义可落地的软件架构。本 skill 管**应用层代码怎么组织**：状态机、订阅-发布、生产者-消费者、定时器模拟任务（前后台）、FreeRTOS 架构。驱动与命名细节分别遵守 `driver-layer-architecture` 与 `embedded-c-naming`。

## 命名规范（本 skill 的代码必须遵守）

> **先定规则，再看例子。** 本 skill 各节代码块全部遵循 `embedded-c-naming`。凡是与 `embedded-c-naming` 冲突的写法，以其为准；本节只补充"分层 + 变量"两件事。

### 分层前缀（模块前缀 PREFIX-01/02/03）

应用层/中间件层**没有** `<芯片系列>_<外设>_`（那是 Driver 层）。本层的公开符号前缀 = **`<层缩写>_<模块名>_`**。各层建议前缀如下（选其一，全项目统一）：

| 层                | 目录            | 推荐前缀           | 表示                                        |
| ---------------- | ------------- | -------------- | ----------------------------------------- |
| App（应用层）         | `App/`        | `app_`         | 产品业务流程编排（`app_fsm_`、`app_run_`）           |
| Module FSM       | `App/`或`Mdl/` | `<模块>_`        | 模块流程（`key_`、`uart_`、`motor_`）             |
| Middleware（中间件层） | `Middleware/` | `<组件>_`        | 通用能力（`sched_`、`evtq_`、`evt_bus_`、`ring_`） |
| Driver（驱动抽象层）    | `Driver/`     | `<芯片系列>_<外设>_` | 见 `driver-layer-architecture`             |
| Port/BSP（板级移植层）  | `Port/`       | `bsp_`         | 唯一碰硬件细节（`bsp_systick_`）                   |

> **规则：一个模块一个前缀，全项目统一。** 例：事件队列一律 `evtq_`，事件总线一律 `evt_bus_`，调度器一律 `sched_`，系统节拍一律 `bsp_systick_`。不要同一模块换着叫 `bus_`/`event_bus_`/`evt_bus_`。

### 变量标签（VAR-01~05, VAR-03）

**字长标签** 是变量命名基准（形参除外，见 VAR-06）。嵌入式常用对照：

| C 类型                 | 变量前缀 | C 类型               | 变量前缀 |
| -------------------- | ---- | ------------------ | ---- |
| `unsigned char`      | `uc` | `signed char`      | `sc` |
| `unsigned short`     | `us` | `signed short`     | `ss` |
| `unsigned int`       | `uw` | `signed int`       | `sw` |
| `unsigned long long` | `ul` | `signed long long` | `sl` |
| `float`              | `f`  | `double`           | `d`  |

**修饰符**：`p`=指针、`a`=数组。`puc`=`unsigned char *`，`auc`=`unsigned char[]`。

**作用域前缀**：全局 `g_`、文件静态 `gs_`、局部静态 `s_`、局部（无前缀）。一个变量 = `作用域？+ 修饰符 + 标签_蛇形名`，例：`gs_uc_head`（文件静态 + unsigned char）、`pstEvt`（形参指针，见下）。

### typedef 形参（VAR-06，唯一例外）

形参类型用**位宽别名**（`u8`/`u16`/`u32`），命名用**位宽标签 + 驼峰**：`u8 u8Data`、`u32 u32Len`、`u8 *pu8Data`。**仅形参**允许；函数体、结构体成员、返回值、静态变量一律标准 C 类型 + 字长标签。

### 变量命名硬规则（本 skill 再三强调）

- **非形参禁止 `u8`/`u32` 等位宽别名与 stdint 类型**：一律写 `unsigned char`/`unsigned int`/`unsigned short`。
- **非形参禁止驼峰**（结构体/联合体成员、局部、静态、全局）：一律蛇形。`st_hdl` 非 `stHdl`，`uc_head` 非 `ucHead`。
- **函数指针成员**：`pf` 前缀 + 蛇形小写；返回值用标准 C 类型（`unsigned int (*pf_get_tick)(void)`），仅形参保留位宽。
- **指向结构体的指针成员**：`pst_` 前缀；指向基础类型用 `p`/`p<标签>`。

### 类型 / 枚举 / 函数 / 宏（速记）

| 类别          | 格式                                      | 示例                                 |
| ----------- | --------------------------------------- | ---------------------------------- |
| 结构体 typedef | `typedef struct <标签> {} s首词大写_其余蛇形_t;`  | `sApp_event_t`                     |
| 枚举 typedef  | `typedef enum <标签> {} e首词大写_其余蛇形_t;`    | `eKey_state_t`                     |
| 枚举值         | `e` + 首词首字母大写 + `_` + 蛇形；带完整模块前缀；首个显式赋值 | `eKey_state_idle = 0`              |
| 回调 typedef  | `<前缀>_<事件>_callback_t`                  | `sched_task_callback_t`            |
| 公开函数        | `<前缀>_<动词>_<名词>`                        | `evtq_post`、`key_fsm_handle_event` |
| 静态/内部函数     | `prv` 前缀                                | `prvKeyStartDebounce`              |
| 宏           | `全大写_SNAKE_CASE`                        | `EVTQ_MAX`                         |
| 头文件保护       | `<文件>_H`                                | `KEY_FSM_H`                        |

### 风格速记（STY/COM/HDR）

- 缩进 4 空格禁 TAB；`if/while`、函数体 `{` 下一行独占。
- 注释用英语、只 `/* */`、置于代码上方；`//` 禁止。
- 尤达条件：`if(eKey_event_scan == eEvent)`。
- 每个 `.c` 带标准文件头（版权+fileName/Author/version/function+Notes+Changelog）；`.h` 直接以 `#ifndef` 开头，无文件头。
- include 顺序：自己的头 → 本模块依赖头 → HAL/main → 系统头。`.c` 顶部先前向声明全部函数（`prv` 静态在前、公开在后）。

### 命名审查点（本 skill 生成代码自查）

- [ ] 每个公开符号是否带 `<层缩写>_<模块名>_` 前缀？
- [ ] 非形参变量是否用字长标签 + 蛇形（无 `u8`/`u32`/stdint）？
- [ ] 形参是否位宽别名 + 驼峰？
- [ ] 结构体成员是否蛇形、指针成员是否 `pst_`/`pf`/`p` 前缀？
- [ ] typedef 是否 `s/e/u首词大写_其余蛇形_t`、枚举值是否 `e` 前缀 + 模块前缀 + 显式赋值？
- [ ] 注释是否英语 `/* */`、缩进 4 空格，== 常量在左？（§命名规范）
- [ ] `.c` 是否有标准文件头、.h 是否直接 `#ifndef`？

## 架构全景

```
┌─────────────────────────────────────────────────────┐
│  APP（应用层）    业务任务、状态机、策略、产品流程       │
│                   可调用 Middleware API / Driver 接口   │
├─────────────────────────────────────────────────────┤
│  Middleware（中间件层）                                │
│    FreeRTOS/CMSIS-RTOS（调度与 IPC）                  │
│    EventBus / SoftTimer / 协议栈 / 文件系统 / 日志      │
│    可调用 Driver 接口                                  │
├─────────────────────────────────────────────────────┤
│  Driver（驱动抽象层）  LED/UART/SPI/ADC/存储，统一接口   │
│                       与具体芯片无关                    │
├─────────────────────────────────────────────────────┤
│  Port/BSP（板级移植层） HAL、引脚、时钟、外设初始化        │
│                       唯一允许碰硬件细节的地方            │
├─────────────────────────────────────────────────────┤
│  Hardware  MCU/外设/引脚                               │
└─────────────────────────────────────────────────────┘
```

**代码目录（分层落地）：**
```
APP/                业务模块与任务
Middleware/FreeRtos/
Middleware/EventBus、SoftTimer、Shell、协议、日志
Driver/
Port/
Core/               main、中断向量、cube 生成代码
```

> 分层的核心：**下层不知道上层**。Driver 只做硬件能力（读/写/启动/停止）；中间件只做通用能力（调度/通信/解析）；APP 只做产品流程编排。命名细节遵循 `embedded-c-naming`（见上文"命名规范"）。

## 1. 状态机（FSM）— 裸机业务骨架

### 1.1 五要素

状态机五要素：**状态 state、事件 event、动作 action、转移 transition、当前状态 current**。

```
当前状态 + 事件 -> 动作 + 下一个状态
```

### 1.2 最小实现：enum + switch

每个模块一个 enum 状态枚举 + 一个 switch(state) 分发函数。

```c
/* key_fsm.h — 模块状态机，命名遵守 embedded-c-naming */
typedef enum eKey_state
{
    eKey_state_idle = 0,          /* First enum explicitly assigned (ENM-03). */
    eKey_state_debounce,
    eKey_state_click,
    eKey_state_long_press
} eKey_state_t;

typedef enum eKey_event
{
    eKey_event_scan = 0,
    eKey_event_timeout,
    eKey_event_release
} eKey_event_t;

/* Timing macros (MAC-06: no naked numbers). */
#define KEY_DEBOUNCE_MS   10u
#define KEY_LONG_PRESS_MS 800u

void key_fsm_init(void);
void key_fsm_handle_event(eKey_event_t eEvent);
```

```c
/* key_fsm.c — ISR only records the event; main loop decides state change. */
#include "key_fsm.h"

static eKey_state_t s_e_current_state = eKey_state_idle;

void key_fsm_init(void)
{
    s_e_current_state = eKey_state_idle;
}

void key_fsm_handle_event(eKey_event_t eEvent)
{
    switch(s_e_current_state)
    {
    case eKey_state_idle:
        if(eKey_event_scan == eEvent)
        {
            s_e_current_state = eKey_state_debounce;   /* Transition. */
        }
        break;
    case eKey_state_debounce:
        if(eKey_event_timeout == eEvent)
        {
            s_e_current_state = eKey_state_click;      /* Debounce done. */
        }
        break;
    default:
        break;
    }
}
```

> **Event 从哪来（vsdx 三来源）：**
> 1. **主循环轮询**：读按键、读传感器，生成事件
> 2. **中断置标志**：中断里只置 `volatile` 标志，主循环检查标志生成事件
> 3. **定时器计时**：用定时器判断是否超时（timeout），生成事件
>
> **分工铁律：中断只记录"发生了什么"；主循环决定"该怎么切状态"。**

### 1.3 工程版本：多 FSM + 分层 + 事件分发

单状态机只够玩具。工程要关心的四件事（vsdx）：
1. 系统有哪些模块
2. 每个模块自己的流程是什么
3. 模块之间如何通过事件协作
4. 事件驱动放在哪一层

工程版要达成：**多 FSM + 分层 + 事件分发** → 可维护、可扩展、可排障。

```
APP FSM（产品级编排：什么时候允许启动、故障如何降级）
  │ 调用/事件
Module FSM（模块流程：电机怎么启动、协议怎么收包）   ← 每个外设/协议一个
  │ 调用
Driver（硬件能力：读、写、启动、停止）
```

**为什么要有多个状态机（vsdx）：** 因为很多流程是并行的——通信协议正在收帧、电机正在软启动、按键正在消抖、系统处于运行模式。一个全局大状态机无法表达并发。

**正确拆法（vsdx 实例）：**

| FSM         | 状态链                                                                                                             | 职责      |
| ----------- | --------------------------------------------------------------------------------------------------------------- | ------- |
| `uart_fsm`  | `eUart_state_wait_head -> eUart_state_recv -> eUart_state_crc -> eUart_state_dispatch`                          | 协议收帧    |
| `motor_fsm` | `eMotor_state_stop -> eMotor_state_starting -> eMotor_state_run -> eMotor_state_stopping -> eMotor_state_fault` | 电机软启停   |
| `key_fsm`   | `eKey_state_idle -> eKey_state_debounce -> eKey_state_click / eKey_state_long_press`                            | 按键消抖/识别 |
| `app_fsm`   | `eApp_state_self_check -> eApp_state_idle -> eApp_state_running -> eApp_state_fault`                            | 系统模式编排  |

**职责划分：**
- **Driver**：只负责硬件能力（读、写、启动、停止）
- **Module FSM**：负责本模块的流程（电机怎么启动、协议怎么收包）
- **APP FSM**：负责产品级编排（什么时候允许启动、故障如何降级）

### 1.4 事件链（vsdx）

```
UART中断 / 按键中断 / 故障检测
    -> 置标志 或 写入事件队列
    -> app_fsm / <module>_fsm 处理
    -> 调用 Driver API
```

### 1.5 状态机的两种实现风格（扩展）

| 风格 | 优点 | 缺点 | 适用 |
|------|------|------|------|
| **switch-case**（1.2） | 简单、直观、可读 | 状态多时代码膨胀 | ≤10 状态 |
| **状态表（state table）** | 数据驱动、易扩展、易做诊断 | 回调间接、调试稍难 | 状态多、需运行时调整 |

状态表实现（事件 -> 动作回调 + 下一状态）：

```c
typedef struct sKey_fsm_trans
{
    eKey_state_t eState;              /* Current state. */
    eKey_event_t eEvent;              /* Trigger event. */
    eKey_state_t eNext;               /* Next state. */
    void        (*pf_action)(void);   /* Transition action. */
} sKey_fsm_trans_t;

static const sKey_fsm_trans_t gs_ast_key_trans[] = {
    { eKey_state_idle,     eKey_event_scan,    eKey_state_debounce, prvKeyStartDebounce },
    { eKey_state_debounce, eKey_event_timeout, eKey_state_click,    prvKeyOnClick },
    /* ... */
};

void key_fsm_handle_event(eKey_event_t eEvent)
{
    unsigned int uw_idx;
    for(uw_idx = 0u; uw_idx < (sizeof(gs_ast_key_trans) / sizeof(gs_ast_key_trans[0])); uw_idx++)
    {
        if((eEvent == gs_ast_key_trans[uw_idx].eEvent) &&
           (s_e_current_state == gs_ast_key_trans[uw_idx].eState))
        {
            if(NULL != gs_ast_key_trans[uw_idx].pf_action)
            {
                gs_ast_key_trans[uw_idx].pf_action();   /* Action. */
            }
            s_e_current_state = gs_ast_key_trans[uw_idx].eNext;   /* Transition. */
            break;
        }
    }
}
```

> 两种风格可混用：小模块用 switch，复杂模块用状态表。状态表天然支持把"哪条规则触发"打印出来排障。
>
> **可直接跑的表驱动参考 demo（含 guard 扩展）见 `references/framework-demo-table/`**：同一套前后台骨架，`key_fsm`（带计时守卫）与 `app_fsm`（纯四元组）都以状态表实现，附 host `test_fsm_tbl.c` 自检（`table-driven FSM self-check OK`）。

## 2. 订阅者-发布者模型（Pub/Sub / 事件总线）

### 2.1 核心思想（vsdx）

> Publisher-subscriber（pub/sub）是一种**用消息完成模块协作**的通信方式。
> - **Publisher** 只负责发出"发生了什么"（不知道谁会收到）
> - **Subscriber** 只负责声明"我对什么感兴趣、收到后怎么处理"
> - **中间通道**负责保存订阅关系，并把消息投递给匹配的订阅者（保存订阅表、完成匹配和分发）
>
> 发布者不需要知道谁会收到消息，订阅者也不需要知道消息是谁发送的。**双方不直接相互调用，只依赖消息约定和通道接口。**

### 2.2 订阅表 + 同步发布（最小实现）

**订阅时写"事件号 + 回调"，发布时按事件号找到所有匹配项，逐个回调**（vsdx）。

```c
/* evt_bus.h — 同步事件总线（中间件层） */
#include "evtq.h"   /* Reuse eEvt_t event ids. */

#define EVT_BUS_MAX_SUB   (eEvt_max * 4u)   /* Max 4 subscribers per event. */

typedef void (*evt_bus_handler_callback_t)(void *pvArg);

typedef struct sEvt_bus_sub
{
    unsigned int              ueEvent;    /* Event id to subscribe to. */
    evt_bus_handler_callback_t pfHandler; /* Callback function. */
    void                     *pvArg;      /* Callback context. */
} sEvt_bus_sub_t;

void evt_bus_init(void);
void evt_bus_subscribe(u32 ueEvent, evt_bus_handler_callback_t pfHandler, void *pvArg);
void evt_bus_publish(u32 ueEvent);   /* Synchronous publish. */
```

```c
/* evt_bus.c */
#include "evt_bus.h"

static sEvt_bus_sub_t gs_ast_sub[EVT_BUS_MAX_SUB];
static unsigned int   gs_uw_sub_cnt;

void evt_bus_init(void)
{
    gs_uw_sub_cnt = 0u;
}

void evt_bus_subscribe(u32 ueEvent, evt_bus_handler_callback_t pfHandler, void *pvArg)
{
    if(gs_uw_sub_cnt < EVT_BUS_MAX_SUB)
    {
        gs_ast_sub[gs_uw_sub_cnt].ueEvent   = ueEvent;
        gs_ast_sub[gs_uw_sub_cnt].pfHandler = pfHandler;
        gs_ast_sub[gs_uw_sub_cnt].pvArg     = pvArg;
        gs_uw_sub_cnt++;
    }
}

void evt_bus_publish(u32 ueEvent)   /* Synchronous: caller runs the callbacks. */
{
    unsigned int uw_idx;
    for(uw_idx = 0u; uw_idx < gs_uw_sub_cnt; uw_idx++)
    {
        if(ueEvent == gs_ast_sub[uw_idx].ueEvent)
        {
            if(NULL != gs_ast_sub[uw_idx].pfHandler)
            {
                gs_ast_sub[uw_idx].pfHandler(gs_ast_sub[uw_idx].pvArg);
            }
        }
    }
}
```

订阅示例（vsdx 的订阅表）：

```c
evt_bus_subscribe(eEvt_key,     led_blink,  NULL);   /* LED.blink();   */
evt_bus_subscribe(eEvt_timeout, motor_stop, NULL);   /* Motor.stop();  */
evt_bus_subscribe(eEvt_uart_rx, ui_update,  NULL);   /* UI.update();   */
```

### 2.3 同步发布 vs 异步发布（vsdx）

| | 同步发布 | 异步发布 |
|---|---|---|
| 机制 | 发布时直接调用回调 | 先入队，再在主循环统一分发 |
| 优点 | 实现简单、延迟低 | 适合中断里只发事件、主循环统一处理 |
| 缺点 | 回调会占用发布者的时间 | 有队列深度/延迟 |
| 适用 | 任务上下文内、回调短 | **ISR 中禁止复杂业务**时 |

> **关键取舍：** 异步发布能避免"在 ISR 中做复杂业务"。中断里只 `evtq_post(...)` 入队，主循环 `evtq_pop(...)` 统一出队回调。

### 2.4 异步事件队列（事件驱动核心，vsdx）

vsdx 的事件队列：`EventBus_Post(src、dst、ID、para)`，环形缓冲 head 写、tail 读，最多 32 条，满则丢弃，**Post 可以在中断里调用**。

```c
/* evtq.h — 中断安全的事件队列（生产者-消费者 + 环形缓冲、SPSC 无锁） */
#include "fw_types.h"   /* u8/u32 param-width aliases (VAR-06, params only). */

#define EVTQ_MAX         32u        /* Queue depth (max 32 per vsdx). */
#define EVTQ_BROADCAST   0xFFu      /* Dst broadcast: fan out to all subscribers. */

typedef enum eEvt
{
    eEvt_none    = 0u,   /* None.          */
    eEvt_key     = 1u,   /* Key event.     */
    eEvt_uart_rx = 2u,   /* UART byte rx.  */
    eEvt_timeout = 3u,   /* Timer timeout. */
    eEvt_fault   = 4u,   /* Fault.         */
    eEvt_max
} eEvt_t;

typedef enum eDst
{
    eDst_app  = 0u,   /* App.  */
    eDst_key  = 1u,   /* Key.  */
    eDst_comm = 2u,   /* Comm. */
    eDst_max
} eDst_t;

typedef struct sEvent
{
    unsigned char uc_dst;   /* Destination (eDst or EVTQ_BROADCAST). */
    unsigned char uc_id;    /* Event id (eEvt). */
    unsigned char uc_src;   /* Source. */
    unsigned char uc_arg;   /* Argument. */
} sEvent_t;

void evtq_init(void);
unsigned char evtq_post(const sEvent_t *pstEvt);   /* Can be called in ISR. */
unsigned char evtq_pop(sEvent_t *pstEvt);          /* Called from main loop. */
```

```c
/* evtq.c — 环形缓冲：head 写、tail 读 */
#include "evtq.h"

static sEvent_t gs_ast_q[EVTQ_MAX];
static volatile unsigned char gs_uc_head;   /* Write pointer (ISR writes). */
static volatile unsigned char gs_uc_tail;   /* Read pointer (main loop reads). */

void evtq_init(void)
{
    gs_uc_head = 0u;
    gs_uc_tail = 0u;
}

unsigned char evtq_post(const sEvent_t *pstEvt)
{
    unsigned char uc_next = (unsigned char)((gs_uc_head + 1u) % EVTQ_MAX);
    if(uc_next == gs_uc_tail)
    {
        return 0u;   /* Full: drop (vsdx: max 32, drop when full). */
    }
    gs_ast_q[gs_uc_head] = *pstEvt;
    gs_uc_head = uc_next;
    return 1u;
}

unsigned char evtq_pop(sEvent_t *pstEvt)
{
    if(gs_uc_head == gs_uc_tail)
    {
        return 0u;   /* Empty. */
    }
    *pstEvt = gs_ast_q[gs_uc_tail];
    gs_uc_tail = (unsigned char)((gs_uc_tail + 1u) % EVTQ_MAX);
    return 1u;
}
```

> **SPSC 单生产者单消费者、无锁：** 一个 ISR 写 head、一个主循环读 tail，天然无竞争。`head`/`tail` 用 `volatile` 修饰即可；**单生产者-单消费者不需要加锁**（扩展：多生产者/多消费者才需要关中断或原子操作保护）。`(x+1) % N` 用 `N` 取模即可，`N=256` 时可用 `u8` 溢出自然回绕（扩展优化）。

**出队后分发（vsdx）：** `Dst = 点对点` 只送给 LED/Motor/comm 其中一个；`Dst = Broadcast` 广播抄送给所有 subscriber。出队后按 `uc_id` 查订阅表调 `<module>_fsm_handle_event`，驱动状态切换。

## 3. 生产者-消费者模型 + 环形缓冲区

### 3.1 模型

**生产者（Producer）** 产生数据，**消费者（Consumer）** 处理数据，中间用**缓冲区（Ring Buffer）** 解耦。嵌入式典型场景：ISR 是生产者、任务/主循环是消费者。

```
ISR（生产） -> 环形缓冲区（解耦） -> 主循环/任务（消费）
```

### 3.2 环形缓冲区（ISR 安全，SPSC 无锁）

vsdx 强调：**环形缓冲区 head 写、tail 读**。经典字节流环形缓冲：

```c
/* ring_buf.h — 字节流环形缓冲，SPSC 无锁 */
#include "fw_types.h"

typedef struct sRing_buf
{
    unsigned char  auc_buf[256];   /* Data area, 256 = u8 natural wraparound. */
    unsigned char  uc_head;        /* Write pointer (producer/ISR). */
    unsigned char  uc_tail;        /* Read pointer (consumer/main loop). */
    unsigned char  uc_full;        /* Full flag. */
} sRing_buf_t;

void ring_buf_init(sRing_buf_t *pstBuf);
unsigned char ring_buf_put(sRing_buf_t *pstBuf, u8 u8Data);
unsigned char ring_buf_get(sRing_buf_t *pstBuf, u8 *pu8Data);
unsigned char ring_buf_is_empty(const sRing_buf_t *pstBuf);
unsigned char ring_buf_is_full(const sRing_buf_t *pstBuf);
```

```c
/* ring_buf.c — 256 字节用 u8 溢出回绕，免取模（扩展优化） */
#include "ring_buf.h"

void ring_buf_init(sRing_buf_t *pstBuf)
{
    pstBuf->uc_head = 0u;
    pstBuf->uc_tail = 0u;
    pstBuf->uc_full = 0u;
}

unsigned char ring_buf_put(sRing_buf_t *pstBuf, u8 u8Data)
{
    if(1u == pstBuf->uc_full)
    {
        return 0u;   /* Full. */
    }
    pstBuf->auc_buf[pstBuf->uc_head] = u8Data;
    pstBuf->uc_head++;                         /* u8 overflow wraps naturally. */
    if(pstBuf->uc_head == pstBuf->uc_tail)
    {
        pstBuf->uc_full = 1u;                  /* Filled up. */
    }
    return 1u;
}

unsigned char ring_buf_get(sRing_buf_t *pstBuf, u8 *pu8Data)
{
    if((pstBuf->uc_head == pstBuf->uc_tail) && (0u == pstBuf->uc_full))
    {
        return 0u;   /* Empty. */
    }
    *pu8Data = pstBuf->auc_buf[pstBuf->uc_tail];
    pstBuf->uc_tail++;
    pstBuf->uc_full = 0u;
    return 1u;
}
```

> **内存序/一致性（扩展）：** 单核 MCU 上，SPSC 无锁环形缓冲是安全的——只要 `head`/`tail` 声明为 `volatile`，编译器不会重排访问。多核/DMA 场景才需考虑内存屏障。ISR 安全的关键：**ISR 里只做 put，绝不 get**（或反过来），保持单生产者单消费者。

### 3.3 典型用例：UART 收帧（vsdx 事件链实例）

```
UART_RX 中断：字节入环形缓冲（生产者）
    -> 主循环：从缓冲取字节，喂给 uart_fsm（eUart_state_wait_head -> eUart_state_recv -> eUart_state_crc -> eUart_state_dispatch）
    -> 组帧完成后发布 eEvt_uart_rx
    -> 订阅者 ui_update()
```

## 4. 定时器模拟任务（前后台执行框架）

### 4.1 思路（vsdx）

用一个**硬件定时器固定产生时间节拍**（如每 1ms 一次中断），在中断里**递减各任务的倒计时计数器**，计数到 0 的**置位就绪标志**，主循环**轮询标志并执行对应任务**。任务还能被更高优先级中断抢占。

```
硬件定时器（1ms 节拍）
    -> ISR：遍历任务表，递减 remaining，到期置 ready=1
    -> 主循环：轮询任务表，执行 ready 的任务，执行完清 ready
```

### 4.2 任务结构体（vsdx）

```c
/* sched.h — 前后台调度器（节拍 = SysTick/定时器）。 */
#include "fw_types.h"

#define SCHED_MAX_TASK   8u

/* Task callback (TYP-05: <prefix>_<event>_callback_t). */
typedef void (*sched_task_callback_t)(void);

typedef struct sSched_task
{
    unsigned int      uw_period;     /* Exec period in ticks. */
    unsigned int      uw_remaining;  /* Ticks left until next fire. */
    volatile unsigned char uc_ready; /* Ready flag: 1 = due. */
} sSched_task_t;

void sched_init(void);
int  sched_add_task(u32 u32Period, sched_task_callback_t pfRun);
void sched_tick(void);   /* Called from ISR (1ms tick). */
void sched_poll(void);   /* Called from main loop. */
```

```c
/* sched.c */
#include "sched.h"

static sSched_task_t        gs_ast_task[SCHED_MAX_TASK];
static sched_task_callback_t gs_apf_run[SCHED_MAX_TASK];
static unsigned int         gs_uw_task_cnt;

void sched_init(void)
{
    unsigned int uw_idx;
    for(uw_idx = 0u; uw_idx < SCHED_MAX_TASK; uw_idx++)
    {
        gs_ast_task[uw_idx].uw_period    = 0u;
        gs_ast_task[uw_idx].uw_remaining = 0u;
        gs_ast_task[uw_idx].uc_ready     = 0u;
        gs_apf_run[uw_idx]               = NULL;
    }
    gs_uw_task_cnt = 0u;
}

int sched_add_task(u32 u32Period, sched_task_callback_t pfRun)
{
    unsigned int uw_idx;
    if((SCHED_MAX_TASK == gs_uw_task_cnt) || (NULL == pfRun) || (0u == u32Period))
    {
        return -1;
    }
    uw_idx = gs_uw_task_cnt++;
    gs_ast_task[uw_idx].uw_period    = u32Period;
    gs_ast_task[uw_idx].uw_remaining = u32Period;
    gs_ast_task[uw_idx].uc_ready     = 0u;
    gs_apf_run[uw_idx]               = pfRun;
    return (int)uw_idx;
}
```

> `remaining` 初始值等于 `period`，系统启动后立刻开始倒计时，到期后重装，循环往复（vsdx）。任务回调用 `sched_add_task(period, callback)` 注册，而非全局数组硬编码。

### 4.3 节拍 ISR

```c
/* bsp_systick.c — 1ms 硬件节拍 ISR — 只递减计数、置标志，不做业务。 */
#include "sched.h"

void bsp_systick_isr(void)
{
    sched_tick();
}

/* sched_tick() 内部：遍历任务表，递减 remaining，到期重装并置 ready。 */
```

```c
/* sched_tick() — ISR context, no business logic. */
void sched_tick(void)
{
    unsigned int uw_idx;
    for(uw_idx = 0u; uw_idx < gs_uw_task_cnt; uw_idx++)
    {
        if(0u == gs_ast_task[uw_idx].uc_ready)
        {
            if(0u == gs_ast_task[uw_idx].uw_remaining)
            {
                gs_ast_task[uw_idx].uw_remaining = gs_ast_task[uw_idx].uw_period;  /* Reload. */
                gs_ast_task[uw_idx].uc_ready     = 1u;                             /* Set ready. */
            }
            else
            {
                gs_ast_task[uw_idx].uw_remaining--;
            }
        }
    }
}
```

### 4.4 主循环调度

```c
/* sched_poll() — main loop context, execute ready tasks then clear flag. */
void sched_poll(void)
{
    unsigned int uw_idx;
    for(uw_idx = 0u; uw_idx < gs_uw_task_cnt; uw_idx++)
    {
        if(1u == gs_ast_task[uw_idx].uc_ready)
        {
            gs_ast_task[uw_idx].uc_ready = 0u;              /* Clear first, then run. */
            if(NULL != gs_apf_run[uw_idx])
            {
                gs_apf_run[uw_idx]();                       /* Run task. */
            }
        }
    }
}

/* sched_run() — 主循环入口：poll 任务 + 其它主循环逻辑。 */
void sched_run(void)
{
    for(;;)
    {
        sched_poll();
        /* ... other main-loop work ... */
    }
}
```

### 4.5 任务优先级与重叠处理（vsdx 关键问题）

> **vsdx 问题：** 均到期了重叠了，1ms 内 3 个均执行，还是设置优先级并挂起其他的低优先级任务？

**两种策略：**
1. **全部执行（按表序）**：重叠时按任务表顺序依次执行。简单，但长任务会阻塞后续任务。
2. **优先级抢占**：任务表按优先级排列，高优先级任务到期时，低优先级任务顺延（挂起到下次节拍）。保证实时性。

| 任务 | 周期 | 建议 |
|------|------|------|
| 电机控制（速度环） | 1ms | 最高优先级，禁止被长任务阻塞 |
| 传感器采集（数据读取） | 10ms | 中高 |
| 通信（数据发送） | 50ms | 中 |
| 人机/日志（显示、打印） | 100ms+ | 低，可顺延 |

> **经验：** 前后台框架下，**最坏情况执行时间（WCET）必须小于节拍周期**。高优先级任务放任务表前面，主循环顺序执行天然带优先级；若需严格实时，用 FreeRTOS（见 §5）。

## 5. FreeRTOS 架构

### 5.1 用 FreeRTOS 先定什么（vsdx）

> 用 FreeRTOS 做工程首先要定的就是整体代码架构：**软件怎么分层、任务怎么划分、中断怎么接入、模块怎么通信、资源归谁所有**。
> FreeRTOS 本身属于 **middleware（中间件）**：提供任务、延时、队列、信号量、互斥量等能力，**不写产品业务，也不直接操作板级引脚**。

### 5.2 任务拆解：按业务边界 + 实时性（vsdx）

先明确系统有哪些**独立的任务**在跑：

| 任务 | 类型 |
|------|------|
| adc 采样 | 周期性 |
| OLED 显示 | 周期性 |
| 串口接收与解析 | 事件驱动 |
| 按键处理 | 事件驱动 |
| 报警处理 | 条件触发 |
| LED 状态指示 | 周期性 |

任务优先级按实时性分层（vsdx）：

| 优先级 | 任务类型 | 示例 |
|--------|---------|------|
| **高** | 强实时任务 | 电机环、保护、急停 |
| **中高** | 周期采集 | adc、按键扫描、传感器数据 |
| **中** | 通信协议 | UART/CAN/网络收发解析 |
| **低** | 人机与日志 | Shell、ui、打印 |
| **最低** | Idle | 系统空闲钩子、喂狗补充（系统自带） |

### 5.3 中断架构：中断生产、任务消费（vsdx）

```
硬件中断 -> ISR（读硬件、清中断、发 IPC_ISR，短小）
    ->（队列_ISR / 信号量_ISR / 任务通知_ISR）
    -> 业务任务（完整业务处理，可阻塞可复杂）
```

**ISR 中需要避免（vsdx）：**
- 长时间解析协议
- 浮点大计算
- `printf`（重定时，非可重入）
- 阻塞 API（`osDelay`、等队列）
- 直接调用另一模块的大段业务函数

**三个经典接入（vsdx 实例）：**

| 中断 | ISR 做（生产） | 任务做（消费） |
|------|---------------|----------------|
| `UART_RX 中断` | 字节入环形缓冲 + 给信号量 | 通信任务组帧解析 |
| `EXTI 按键中断` | 记录边沿时间 + 给信号量 | 输入任务消抖与事件 |
| `ADC_DMA 完成中断` | 释放二值信号量 | 采集任务做滤波/换算 |

### 5.4 运行的四类上下文（vsdx）

| 上下文 | 特征 |
|--------|------|
| **业务任务 Task** | 执行用户业务逻辑，可被中断打断，上下文含寄存器、栈、PC |
| **中断 ISR(IRQ)** | 硬件中断处理程序，优先级高于任务，上下文仅保存必要寄存器 |
| **定时器回调 Timer CB** | 定时器到期后执行，运行于中断或软中断上下文，**回调简短快速** |
| **main 启动** | 系统启动入口，执行硬件初始化、OS 启动，上下文为启动栈，后切换至任务 |

### 5.5 中断优先级配置（vsdx + 扩展）

> 会调用 FreeRTOS `FromISR` API 的中断优先级必须落在内核允许范围内（`configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY` 以内）。**更高优先级的中断可以打断 RTOS，但不能调用 RTOS API。**
>
> 设计文档中应写清楚：**哪些 IRQ 会唤醒任务，哪些硬件需要极速处理。**

```c
/* FreeRTOSConfig.h — 中断优先级配置 */
#define configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY  5   /* FromISR API 可用范围 */
#define configKERNEL_INTERRUPT_PRIORITY               15   /* 内核自身 */
#define configMAX_SYSCALL_INTERRUPT_PRIORITY          (configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY << (8 - configPRIO_BITS))
```

> **规则（扩展）：** 需要调用 `xQueueSendFromISR`/`xSemaphoreGiveFromISR` 的中断，其优先级必须数值**低于或等于** `configMAX_SYSCALL_INTERRUPT_PRIORITY`（数值小=优先级高）；不需要调 RTOS API 的极速中断可更高，但会抢占 RTOS 调度。

### 5.6 模块间通信与资源共享（vsdx + 扩展）

**任务间通信（IPC）用 FreeRTOS 原语，模块间通信用 EventBus（§2）：**

| 场景 | 工具 | 说明 |
|------|------|------|
| 数据流（流式字节） | 队列 / 流缓冲 | UART 字节流 |
| 事件通知 | 队列 / 任务通知 | ISR -> 任务 |
| 互斥访问共享资源 | 互斥量（Mutex） | 同一外设多任务访问 |
| 计数同步 | 信号量 | ADC_DMA 完成 -> 采集任务 |
| 模块解耦 | EventBus（§2） | 订阅-发布，业务间解耦 |

**资源归谁所有（vsdx）：** 每个外设/资源有唯一 owner——谁初始化、谁独占访问、谁负责释放。共享资源用 Mutex 保护，ISR 与任务共享的用关中断或 FromISR 安全 API。

### 5.7 模块解耦总则（vsdx）

> **模块解耦：接口设计与依赖管理。** 模块之间只依赖**接口**（头文件声明的 API），不依赖**实现**。改一个模块的内部实现不影响其他模块；模块间的协作走 EventBus 或明确的下层接口调用。

## 分层判定速查

| 想做的事 | 用哪个架构/层 |
|---------|--------------|
| 模块内部流程控制（协议收帧/电机启停/按键消抖） | 状态机 FSM（§1） |
| 产品级模式编排（自检/运行/故障） | 上层 APP FSM |
| 模块间解耦、广播通知 | Pub/Sub 事件总线（§2） |
| 中断产生数据、主循环处理 | 生产者-消费者 + 环形缓冲（§3） |
| 裸机周期性任务调度 | 定时器模拟任务/前后台（§4） |
| 需要阻塞、严格实时、多任务并行 | FreeRTOS 任务（§5） |
| 硬件读写 | Driver 层（见 driver-layer-architecture） |
| 命名 | embedded-c-naming |

## 与其它 skill 的关系

- **驱动分层**（Reg/Driver/Handler/Instance）→ `driver-layer-architecture`
- **符号命名**（宏/类型/函数/变量/风格）→ `embedded-c-naming`
- 本 skill 专注 **APP + Middleware 层的组织**：状态机、事件驱动、任务调度、RTOS 架构。
- 三者同时触发：先定分层（driver-layer-architecture），再定业务架构（本 skill），最后定命名（embedded-c-naming）。

### 与 driver-layer-architecture 的分工边界（两个易混点）

**① "Driver" 一词两层含义，按层级语境区分：**

| 出现位置 | Driver 指什么 | 归属 skill |
|---------|--------------|-----------|
| 驱动栈内（Reg→Driver→Handler→Instance） | **总线原语层**（只收发字节，不解析协议） | `driver-layer-architecture` |
| 分层全景（APP→Middleware→**Driver**→Port） | **驱动抽象层**（容纳整个驱动栈） | 本 skill |

**② 状态机归属（Handler 的"协议状态机" vs 本 skill 的 Module FSM）——按"有没有状态可保存"分：**

- **驱动内部协议时序**（如 SPI 驱动的收发状态、I2C 总线时序）——纯驱动事务，无产品业务状态 → **Handler 层**，见 `driver-layer-architecture`。
- **模块级业务流程**（`uart_fsm` 协议收帧→CRC→分发、`motor_fsm` 启停、`key_fsm` 消抖）——描述**产品某模块的流程**，状态属于业务 → **Module FSM（本 skill）**。
- 分界线：这个状态机描述的是**驱动事务**还是**业务模块流程**。前者归 Handler，后者归 Module FSM。
- 常见工程：`uart_fsm`/`key_fsm`/`motor_fsm` 在 APP 侧（本 skill），驱动内部只做字节收发（driver-layer）。

> 一句话：**驱动层管"怎么读写"，中间件/APP 管"什么时候读、读到了怎么办"。** 驱动栈负责字节与寄存器的正确性，FSM/EventBus 负责业务编排。边界不清时问：去掉这个状态机，驱动还能不能正常收发字节？能 → 状态机属于业务层（本 skill）。

## 审查 Checklist

> 审查裸机/RTOS 应用层架构是否清晰。

### 命名
- [ ] 每个公开符号是否带 `<层缩写>_<模块名>_` 前缀？（§命名规范）
- [ ] 非形参变量是否字长标签 + 蛇形，无 `u8`/`u32`/stdint？（§命名规范 / embedded-c-naming）
- [ ] 形参是否位宽别名 + 驼峰？（§命名规范）
- [ ] 结构体成员蛇形、指针成员 `pst_`/`pf`/`p` 前缀？（§命名规范）
- [ ] typedef `s/e/u首词大写_其余蛇形_t`、枚举值 `e` 前缀+模块前缀+显式赋值？（§命名规范）
- [ ] 注释英语 `/* */`、缩进 4 空格、== 常量在左？（§命名规范）
- [ ] `.c` 有标准文件头、`.h` 直接 `#ifndef`？（§命名规范 / embedded-c-naming）

### 状态机
- [ ] 模块是否按业务边界拆成多个 FSM，而非一个全局大状态机？（§1.3）
- [ ] 中断是否只置标志/入队，状态切换是否都在主循环/任务？（§1.2）
- [ ] 每个 FSM 是否 enum + switch（或状态表），状态/事件/转移是否齐全？
- [ ] 驱动/模块 FSM/APP FSM 三层职责是否分离？（§1.3）

### 订阅-发布
- [ ] 发布者是否不感知订阅者、订阅者是否不感知发布者？（§2.1）
- [ ] 订阅表是否按"事件号 + 回调 + 上下文"组织？（§2.2）
- [ ] ISR 中是否只用异步 post，不在中断里跑复杂回调？（§2.3）

### 生产者-消费者
- [ ] 环形缓冲是否 SPSC 无锁、head/tail 是否 volatile？（§3.2）
- [ ] ISR 是否只生产、主循环/任务只消费？（§3.3）
- [ ] 满时处理（丢弃/覆盖/阻塞）是否明确？（§2.4/3.2）

### 前后台调度
- [ ] 任务表是否 `{period, remaining, ready}` 三字段？（§4.2）
- [ ] 节拍 ISR 是否只递减计数/置标志，不执行任务？（§4.3）
- [ ] 主循环是否先清 ready 再执行任务？（§4.4）
- [ ] 任务 WCET 是否小于节拍周期？重叠处理策略是否明确？（§4.5）

### FreeRTOS
- [ ] 任务是否按业务边界 + 实时性拆分，优先级分层？（§5.2）
- [ ] ISR 是否短小、只发 FromISR API？（§5.3）
- [ ] 调 FromISR API 的中断优先级是否 ≤ configMAX_SYSCALL_INTERRUPT_PRIORITY？（§5.5）
- [ ] 每个资源是否有唯一 owner？共享资源是否用 Mutex？（§5.6）
- [ ] 四类上下文（Task/ISR/Timer CB/main）是否区分清楚？（§5.4）

## 6. 参考 Demo（第一版，可编译基线）

> 可运行的裸机业务骨架，作为**迭代基线**。完整源码在 `references/framework-demo-v1/`，本 skill 后续对分层/命名的优化以它为起点。**本章为导航，不复制代码**；逐字源码见 references。

### 6.1 目标与范围

一个**前后台 + 状态机**的裸机业务架构骨架：硬件节拍驱动任务调度、异步事件队列连接中断与主循环、同步事件总线解耦模块、多个状态机编排业务。**不含真实硬件**：`bsp_systick` 是唯一板级移植点，按键读走 `KEY_DRV_READ()` 宏占位。

### 6.2 分层与职责（本 demo 的落地）

```
App           app_fsm / key_fsm / app_run    业务编排、状态机、事件分发
Middleware    sched    前后台调度器
              evt_bus  同步 订阅-发布（模块解耦）
              evtq     异步 事件队列（ISR→主循环，SPSC）
              fw_types 形参位宽别名（u8/u32，仅形参）
Core/Port     bsp_systick 硬件节拍（唯一移植点）
```

| 文件 | 层 | 职责 | 关键 API |
|------|----|------|---------|
| `app_run.c/h` | App | 装配层：注册任务、订阅、出队分发到各 FSM | `app_run_init`、`app_run_dispatch` |
| `app_fsm.c/h` | App | 产品级模式 FSM（自检/空闲/运行/故障） | `app_fsm_handle_event`、`app_fsm_get_state` |
| `key_fsm.c/h` | App | 按键消抖模块 FSM（消抖/单击/长按） | `key_fsm_poll`、`key_fsm_handle_event` |
| `sched.c/h` | Middleware | 前后台调度：tick 置就绪、poll 执行 | `sched_add_task`、`sched_tick`、`sched_poll`、`sched_run` |
| `evt_bus.c/h` | Middleware | 同步 订阅-发布 | `evt_bus_subscribe`、`evt_bus_publish` |
| `evtq.c/h` | Middleware | 异步事件队列（SPSC 环形，ISR 安全） | `evtq_post`、`evtq_pop` |
| `fw_types.h` | Middleware | `u8/u16/u32` 形参位宽别名（仅形参） | — |
| `bsp_systick.c/h` | Core | 1ms 硬件节拍抽象（移植点） | `bsp_systick_start`、`bsp_systick_isr` |

### 6.3 事件链（本 demo 的核心闭环）

```
① 硬件节拍 ISR                 ② 用户中断（按键/串口/故障）
        │  sched_tick()                 │  evtq_post()（ISR 安全）
        ▼                               ▼
   sched 递减 remaining,到期置 ready     evtq 环形缓冲（SPSC 无锁）
        │                               │
主循环 sched_run()  ── poll 执行到期任务──┼──> app_run_dispatch() 出队
        │                                       │ 查 dst/id
        ▼                                       ▼
   周期任务(如 key_fsm_poll)              key_fsm / app_fsm_handle_event
        └───────── 调 Driver 接口 / 切状态 ─────────┘
```

### 6.4 关键决策（后续迭代可复盘的点）

- **中断只记录"发生了什么"**：任何事件先 `evtq_post` 入队，绝不在中断跑业务/回调。
- **前后台与事件队列配合**：`sched_tick`（ISR）只递减计数置 `ready`；`sched_poll`（主循环）先清标志再执行任务。事件队列则让"中断产生的突发事件"与"主循环周期任务"统一在主循环消费。
- **同步总线 vs 异步队列并存**：`evt_bus`（同步）给任务/主循环内解耦；`evtq`（异步）给中断到主循环跨上下文。**ISR 只许 `evtq_post`，绝不在 ISR 跑 `evt_bus_publish` 的回调。**
- **多 FSM 而非一个全局大状态机**：`key_fsm`（模块流程）与 `app_fsm`（产品编排）分离，职责不同。
- **按表序即优先级**：任务越早加入调度表越先执行（高优先级任务先注册）。

### 6.5 自检（host，无需 MCU）

```bash
cd references/framework-demo-v1
gcc -std=c99 -Wall -Wextra -I Middleware -I App -I Core test_framework.c \
    Middleware/sched.c Middleware/evtq.c Middleware/evt_bus.c App/key_fsm.c -o test_fw
./test_fw   # 期望输出: framework self-check OK
```

覆盖：`sched` tick/poll 就绪与执行、`evtq` SPSC 回绕与满丢弃、`evt_bus` 订阅发布、`key_fsm` 消抖→单击转移。

### 6.6 移植

换芯片/定时器，只改 `Core/bsp_systick.c`：把目标芯片的 1ms 定时器初始化写好，并在其中断里调用 `sched_tick()`。其余框架层与芯片无关。

### 6.7 迭代方向（下一版候选）

- **分层命名细化**：各层前缀的边界与例外（如 Driver 的 `<芯片>_<外设>_` 与 Port 的 `bsp_` 如何清晰分流）。
- **调度器增强**：优先级抢占 / 任务死线 / WCET 预算。
- **FSM 状态表化**：把 `key_fsm`/`app_fsm` 从 switch-case 改为状态表（数据驱动，见 §1.5），便于诊断。**已落地**：见 `references/framework-demo-table/`（表驱动参考 demo）。
- **事件负载扩展**：`sEvent_t.uc_arg` 当前单字节，可扩展为指针携上下文。
- **FreeRTOS 迁移**：同一套分层切到 FreeRTOS 任务（见 §5）。

## 参考文件

- 架构全景与代码目录：本文件 §架构全景
- 命名规范：本文件 §命名规范（分层 + 变量）；完整规则 `embedded-c-naming`
- 状态机 / 事件总线 / 环形缓冲 / 前后台 / FreeRTOS 完整代码：本文件各节代码块
- 可编译完整工程（前后台 + 状态机骨架）：`references/framework-demo-v1/`
- **可编译表驱动工程（状态表化参考 demo，含 guard）**：`references/framework-demo-table/`
- 驱动分层细节：`driver-layer-architecture` skill
- 命名规范细节：`embedded-c-naming` skill

## 扩展资料来源

本 skill 基于 `嵌入式软件架构设计.vsdx` 整理，并按以下资料扩展：

- 事件驱动微内核/事件总线：AnboPeng/Anbo-MOS（裸机异步事件总线、故障管理）
- 层次状态机与事件驱动框架：QP™ State Machine Frameworks（Samek）
- 嵌入式 C 设计模式（观察者/回调+上下文）：Douglass《Design Patterns for Embedded Systems in C》
- 无锁环形缓冲 / SPSC ISR 安全：micoring（C99 ISR-safe ring buffer）、Zephyr rtio_spsc
- FreeRTOS 任务划分与中断优先级：FreeRTOS 官方文档、社区工程实践
- 观察者模式 ESP32 实现：StanKarpikov/esp32_observer_pattern



