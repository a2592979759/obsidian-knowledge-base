---
tags:
  - 数据结构
  - 白告月当空照
  - 皓月当空照
  - 算法
date: 2026-09-02
---

# 白告月当空照 —— C 语言数据结构集


> **来源**：整理自 `structData.pdf`。仅收录 **C 语言**实现；剔除书中的 C++ 附录（第 30 页起）、Java 附录（第 41 页起），以及**第七章「算法题及对应代码大全」**（习题集，非基础结构定义）。
>
> **组织**：每个数据结构为一个小节，内含「文字说明 + `.h` 接口 + `.c` 实现」三段。`.h` 与 `.c` 以代码块形式完整嵌入本文档，不另拆文件；同模块两段即可构成可编译单元。
>
> **命名**：全部符号遵守 `embedded-c-naming` 规范 —— 形参用位宽类型（`u32`/`s32`/`u8`）＋位宽驼峰名（`u32Idx`）；非形参（成员/局部/返回值）用标准 C 类型＋字长蛇形名（`uw_len`）；typedef 以 `_t` 结尾；静态函数 `prv` 前缀；`.c` 带标准文件头、`.h` 直接以保护符开头；注释一律英文 `/* */`。
>
> **编译**：全部模块已用 `gcc -std=c99 -Wall -Werror` 独立编译验证通过；各 `.h`/`.c` 可直接落盘成同名文件编译。

---

## 目录

| # | 模块 | 说明 | 复杂度要点 |
|---|------|------|-----------|
| — | 公共类型 ds_types.h | 位宽类型别名（仅形参可用） | — |
| 1 | 顺序表 seq_list | 连续存储线性表（动态数组） | 随机 O(1)，插删 O(n) |
| 2 | 非循环单链表 slist | 带头结点的向前链 | 头尾 O(1) |
| 3 | 循环单链表 clist | 尾结点指回头结点 | 尾插 O(1) |
| 4 | 非循环双链表 dlist | 前后双链，带头结点 | 双向遍历 |
| 5 | 循环双链表 cdlist | 首尾相接的双链 | 双向 O(1) |
| 6 | 顺序栈 seqstack | 数组栈，栈顶指针 | 进出 O(1) |
| 7 | 链栈 linkstack | 链表栈，栈顶即头 | 进出 O(1) |
| 8 | 循环队列 seqqueue | 数组队列，计数判满 | 进出 O(1) |
| 9 | 链队 linkqueue | 链表队列，头尾指针 | 进出 O(1) |
| 10 | 二叉树 btree | 二叉链表建树 | 遍历 O(n) |
| 11 | 二叉排序树 bst | 左小右大，含查/插/删 | 均摊 O(log n) |
| 12 | 堆 heap | 大顶堆数组，上浮/下沉 | 建堆 O(n) |
| 13 | 并查集 unionfind | 集合合并 + 路径压缩 | 反阿克曼 |
| 14 | 排序算法 sort | 插/希/冒/快/选/堆/归并 | 见各算法 |
| 15 | 查找算法 search | 顺序/二分/排序辅助 | 见各算法 |
| 16 | 图 graph | 邻接矩阵 + DFS/BFS | 见各算法 |

---

## 公共类型 ds_types.h

所有模块共享的位宽类型别名。**注意**：`u8`/`u16`/`u32`/`s32` 这些位宽类型及位宽驼峰名**只能出现在函数形参声明中**（命名规范 VAR-06）；其余任何位置（返回值、局部变量、结构体成员、全局变量）一律使用标准 C 类型 + 字长标签（`unsigned char`/`unsigned int`/`unsigned short`/`int`），命名用蛇形（如 `uw_len`）。

```c
#ifndef DS_TYPES_H
#define DS_TYPES_H

/* Shared scalar type aliases. These bit-width names are legal ONLY in
 * function parameter declarations (naming VAR-06); every other position
 * (return type, local, member, global) must use the standard C types. */

typedef unsigned char u8;
typedef unsigned short u16;
typedef unsigned int u32;
typedef signed int s32;

#endif /* DS_TYPES_H */
```

---

## 1. 顺序表 seq_list

**是什么**：顺序表是用一段连续的内存单元依次存放数据元素的线性表，本质是**动态数组**。元素在逻辑上相邻，物理上也相邻，因此按下标随机访问是 O(1)；但插入/删除需要整体移动后续元素，平均 O(n)。

**核心操作**：初始化 `seqlist_init`、销毁 `seqlist_destroy`、判空 `seqlist_is_empty`、取长度 `seqlist_get_len`、按下标存取 `seqlist_get`/`seqlist_set`、按位置插入 `seqlist_insert`、按位置删除 `seqlist_delete`、按值查找 `seqlist_find`、打印 `seqlist_print`。

**结构体字段**：`psw_aData` 为元素指针（动态分配的数组），`uw_len` 当前元素个数，`uw_cap` 已分配容量。插入前若已满则自动扩容一倍（首满扩容到 4）。

### seq_list.h

```c
#ifndef SEQ_LIST_H
#define SEQ_LIST_H

#include "ds_types.h"

/* Sequentially stored linear table (dynamic array).
 * Public functions are prefixed with the module name `seqlist_`.
 * Scalar params use bit-width type + bit-width camel name (VAR-06).
 * Members use standard C type + word-length snake name (VAR-08). */

typedef struct seq_list
{
    int *psw_aData;         /* element buffer, allocated at init */
    unsigned int uw_len;    /* current element count */
    unsigned int uw_cap;    /* allocated capacity */
} sSeq_list_t;

void         seqlist_init(sSeq_list_t *pst_list, u32 u32Cap);
void         seqlist_destroy(sSeq_list_t *pst_list);
int          seqlist_is_empty(const sSeq_list_t *pst_list);
unsigned int seqlist_get_len(const sSeq_list_t *pst_list);
int          seqlist_get(const sSeq_list_t *pst_list, u32 u32Idx);
int          seqlist_set(const sSeq_list_t *pst_list, u32 u32Idx, s32 s32Elem);
int          seqlist_insert(sSeq_list_t *pst_list, u32 u32Idx, s32 s32Elem);
int          seqlist_delete(sSeq_list_t *pst_list, u32 u32Idx, s32 *ps32Elem);
int          seqlist_find(const sSeq_list_t *pst_list, s32 s32Elem);
void         seqlist_print(const sSeq_list_t *pst_list);

#endif /* SEQ_LIST_H */
```

### seq_list.c

```c
/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: seq_list.c
Author: name
version: V1.0.0
function: Sequentially stored linear table - init, length, get/set, insert,
          delete, find, destroy.
*/
/*
Notes:
1. Dynamic array owned by the caller; memory is allocated at init.
2. Indices are zero-based; valid range [0, len).
*/
/*
Changelog:
1. Initial release.
*/

#include "seq_list.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static int prvEnsureCapacity(sSeq_list_t *pst_list);

void seqlist_init(sSeq_list_t *pst_list, u32 u32Cap)
{
    pst_list->psw_aData = (int *)malloc(sizeof(int) * (size_t)u32Cap);
    pst_list->uw_len = 0u;
    pst_list->uw_cap = (unsigned int)u32Cap;
}

void seqlist_destroy(sSeq_list_t *pst_list)
{
    free(pst_list->psw_aData);
    pst_list->psw_aData = NULL;
    pst_list->uw_len = 0u;
    pst_list->uw_cap = 0u;
}

int seqlist_is_empty(const sSeq_list_t *pst_list)
{
    if(0u == pst_list->uw_len)
    {
        return 1;
    }
    return 0;
}

unsigned int seqlist_get_len(const sSeq_list_t *pst_list)
{
    return pst_list->uw_len;
}

int seqlist_get(const sSeq_list_t *pst_list, u32 u32Idx)
{
    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    return pst_list->psw_aData[(unsigned int)u32Idx];
}

int seqlist_set(const sSeq_list_t *pst_list, u32 u32Idx, s32 s32Elem)
{
    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    pst_list->psw_aData[(unsigned int)u32Idx] = (int)s32Elem;
    return 1;
}

/* Insert a value before position u32Idx (0 <= index <= len). */
int seqlist_insert(sSeq_list_t *pst_list, u32 u32Idx, s32 s32Elem)
{
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx > pst_list->uw_len)
    {
        return 0;
    }
    if(0 == prvEnsureCapacity(pst_list))
    {
        return 0;
    }
    for(uw_i = pst_list->uw_len; uw_i > uw_idx; uw_i--)
    {
        pst_list->psw_aData[uw_i] = pst_list->psw_aData[uw_i - 1u];
    }
    pst_list->psw_aData[uw_idx] = (int)s32Elem;
    pst_list->uw_len++;
    return 1;
}

/* Delete the value at position u32Idx and return it via *ps32Elem. */
int seqlist_delete(sSeq_list_t *pst_list, u32 u32Idx, s32 *ps32Elem)
{
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    *ps32Elem = (s32)pst_list->psw_aData[uw_idx];
    for(uw_i = uw_idx; uw_i < pst_list->uw_len - 1u; uw_i++)
    {
        pst_list->psw_aData[uw_i] = pst_list->psw_aData[uw_i + 1u];
    }
    pst_list->uw_len--;
    return 1;
}

/* Return the index of the first occurrence of s32Elem, or -1 if absent. */
int seqlist_find(const sSeq_list_t *pst_list, s32 s32Elem)
{
    unsigned int uw_i;

    for(uw_i = 0u; uw_i < pst_list->uw_len; uw_i++)
    {
        if(pst_list->psw_aData[uw_i] == (int)s32Elem)
        {
            return (int)uw_i;
        }
    }
    return -1;
}

void seqlist_print(const sSeq_list_t *pst_list)
{
    unsigned int uw_i;

    for(uw_i = 0u; uw_i < pst_list->uw_len; uw_i++)
    {
        printf("%d ", pst_list->psw_aData[uw_i]);
    }
    printf("\n");
}

/* Private: grow the buffer when it is full. Returns 1 on success. */
static int prvEnsureCapacity(sSeq_list_t *pst_list)
{
    int *psw_tmp;
    unsigned int uw_i;
    unsigned int uw_new_cap;

    if(pst_list->uw_len < pst_list->uw_cap)
    {
        return 1;
    }
    uw_new_cap = (0u == pst_list->uw_cap) ? 4u : (pst_list->uw_cap * 2u);
    psw_tmp = (int *)malloc(sizeof(int) * (size_t)uw_new_cap);
    if(NULL == psw_tmp)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < pst_list->uw_len; uw_i++)
    {
        psw_tmp[uw_i] = pst_list->psw_aData[uw_i];
    }
    free(pst_list->psw_aData);
    pst_list->psw_aData = psw_tmp;
    pst_list->uw_cap = uw_new_cap;
    return 1;
}
```

---

## 非循环单链表 slist

**是什么**：单链表用结点链式存储，逻辑相邻但物理不必连续。它引入一个**带头结点**的技巧：头结点本身不存数据，真实数据从 `head->next` 开始。带头结点的好处是「空表和非空表的插入/删除逻辑统一，无需单独处理首结点」。

**核心操作**：初始化 `slist_init`、判空 `slist_is_empty`、取长度 `slist_get_len`、按下标读取 `slist_get`、按位置插入/删除 `slist_insert`/`slist_delete`、按值查找 `slist_find`、打印 `slist_print`、销毁 `slist_destroy`。

**结构体字段**：`sSlist_node_t` 为链表结点，含数据 `sw_data` 和后继指针 `pst_next`；`sSlist_t` 持有头指针 `pst_head`（哑元）和计数 `uw_len`。查找、插入、删除都要遍历到目标位置的前驱。

### slist.h

```c
#ifndef SLIST_H
#define SLIST_H

#include "ds_types.h"

/* Singly-linked list (non-circular) with a dummy head node.
 * The head node itself carries no element; real data starts at head->next.
 * Scalar params use bit-width type + bit-width camel name (VAR-06);
 * members use standard C type + word-length snake name (VAR-08). */

typedef struct slist_node
{
    int sw_data;                    /* element value */
    struct slist_node *pst_next;    /* next node, NULL at tail */
} sSlist_node_t;

typedef struct slist
{
    sSlist_node_t *pst_head;        /* dummy head, lacks element */
    unsigned int uw_len;            /* element count (excluding head) */
} sSlist_t;

void         slist_init(sSlist_t *pst_list);
void         slist_destroy(sSlist_t *pst_list);
int          slist_is_empty(const sSlist_t *pst_list);
unsigned int slist_get_len(const sSlist_t *pst_list);
int          slist_get(const sSlist_t *pst_list, u32 u32Idx);
int          slist_insert(sSlist_t *pst_list, u32 u32Idx, s32 s32Elem);
int          slist_delete(sSlist_t *pst_list, u32 u32Idx, s32 *ps32Elem);
int          slist_find(const sSlist_t *pst_list, s32 s32Elem);
void         slist_print(const sSlist_t *pst_list);

#endif /* SLIST_H */
```

### slist.c

```c
/* Single linked list (non-circular) with dummy head node.
 * Public functions use `slist_` prefix. Indices in the API are zero-based
 * and refer to element positions that exclude the dummy head. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: slist.c
Author: name
version: V1.0.0
function: Singly-linked list with dummy head - init, length, get, insert,
          delete, find, print, destroy.
*/
/*
Notes:
1. The first element is head->next; head->data is unused.
2. Indices are zero-based; valid range [0, len).
*/
/*
Changelog:
1. Initial release.
*/

#include "slist.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sSlist_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sSlist_node_t *pst_node);

void slist_init(sSlist_t *pst_list)
{
    pst_list->pst_head = prvNodeNew(0);
    pst_list->uw_len = 0u;
}

void slist_destroy(sSlist_t *pst_list)
{
    sSlist_node_t *pst_cur = pst_list->pst_head;
    sSlist_node_t *pst_tmp;

    while(NULL != pst_cur)
    {
        pst_tmp = pst_cur;
        pst_cur = pst_cur->pst_next;
        prvNodeFree(pst_tmp);
    }
    pst_list->pst_head = NULL;
    pst_list->uw_len = 0u;
}

int slist_is_empty(const sSlist_t *pst_list)
{
    if(NULL == pst_list->pst_head->pst_next)
    {
        return 1;
    }
    return 0;
}

unsigned int slist_get_len(const sSlist_t *pst_list)
{
    return pst_list->uw_len;
}

int slist_get(const sSlist_t *pst_list, u32 u32Idx)
{
    sSlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < (unsigned int)u32Idx; uw_i++)
    {
        pst_cur = pst_cur->pst_next;
    }
    return pst_cur->sw_data;
}

/* Insert a value before position u32Idx (0 <= index <= len). */
int slist_insert(sSlist_t *pst_list, u32 u32Idx, s32 s32Elem)
{
    sSlist_node_t *pst_pre = pst_list->pst_head;
    sSlist_node_t *pst_new;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx > pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    pst_new->pst_next = pst_pre->pst_next;
    pst_pre->pst_next = pst_new;
    pst_list->uw_len++;
    return 1;
}

/* Delete the element at position u32Idx and store it via *ps32Elem. */
int slist_delete(sSlist_t *pst_list, u32 u32Idx, s32 *ps32Elem)
{
    sSlist_node_t *pst_pre = pst_list->pst_head;
    sSlist_node_t *pst_del;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_del = pst_pre->pst_next;
    *ps32Elem = pst_del->sw_data;
    pst_pre->pst_next = pst_del->pst_next;
    prvNodeFree(pst_del);
    pst_list->uw_len--;
    return 1;
}

/* Return the index of the first occurrence of s32Elem, or -1 if absent. */
int slist_find(const sSlist_t *pst_list, s32 s32Elem)
{
    sSlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i = 0u;

    while(NULL != pst_cur)
    {
        if(pst_cur->sw_data == (int)s32Elem)
        {
            return (int)uw_i;
        }
        pst_cur = pst_cur->pst_next;
        uw_i++;
    }
    return -1;
}

void slist_print(const sSlist_t *pst_list)
{
    sSlist_node_t *pst_cur = pst_list->pst_head->pst_next;

    while(NULL != pst_cur)
    {
        printf("%d ", pst_cur->sw_data);
        pst_cur = pst_cur->pst_next;
    }
    printf("\n");
}

static sSlist_node_t *prvNodeNew(s32 s32Elem)
{
    sSlist_node_t *pst_node;

    pst_node = (sSlist_node_t *)malloc(sizeof(sSlist_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_next = NULL;
    return pst_node;
}

static void prvNodeFree(sSlist_node_t *pst_node)
{
    free(pst_node);
}
```

---

## 循环单链表 clist

**是什么**：循环单链表在普通单链表基础上，把**尾结点的后继指针指向头结点**，使整个链表首尾相接成一个环。这样「沿任一结点都能绕回起点」，且「在尾部插入元素可以直接借助环找到尾结点」。仍带哑元头结点。

**核心操作**：与 `slist` 同名同义（`clist_init`/`clist_insert`/`clist_delete`/`clist_find`/`clist_print` 等），循环的体现主要在「遍历的终止条件是 `指针 == 头结点`」而非「`指针 == NULL`」。

**结构体字段**：结点 `sClist_node_t`（`sw_data` + `pst_next`），容器 `sClist_t`（哑元头 `pst_head` + 计数 `uw_len`）。初始时头结点自指 `head->next = head` 表示空环。

### clist.h

```c
#ifndef CLIST_H
#define CLIST_H

#include "ds_types.h"

/* Circular singly-linked list with a dummy head node.
 * The tail node's next pointer loops back to the head node, forming a ring.
 * Scalar params use bit-width type + bit-width camel name (VAR-06);
 * members use standard C type + word-length snake name (VAR-08). */

typedef struct clist_node
{
    int sw_data;
    struct clist_node *pst_next;
} sClist_node_t;

typedef struct clist
{
    sClist_node_t *pst_head;    /* dummy head; tail->next points back to it */
    unsigned int uw_len;
} sClist_t;

void         clist_init(sClist_t *pst_list);
void         clist_destroy(sClist_t *pst_list);
int          clist_is_empty(const sClist_t *pst_list);
unsigned int clist_get_len(const sClist_t *pst_list);
int          clist_get(const sClist_t *pst_list, u32 u32Idx);
int          clist_insert(sClist_t *pst_list, u32 u32Idx, s32 s32Elem);
int          clist_delete(sClist_t *pst_list, u32 u32Idx, s32 *ps32Elem);
int          clist_find(const sClist_t *pst_list, s32 s32Elem);
void         clist_print(const sClist_t *pst_list);

#endif /* CLIST_H */
```

### clist.c

```c
/* Circular singly-linked list with dummy head node. The last node's next
 * pointer points back to the head node, so the structure forms a ring. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: clist.c
Author: name
version: V1.0.0
function: Circular singly-linked list - init, length, get, insert, delete,
          find, print, destroy.
*/
/*
Notes:
1. head is a dummy node; tail->next == head, forming a ring.
2. Indices are zero-based; valid range [0, len).
*/
/*
Changelog:
1. Initial release.
*/

#include "clist.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sClist_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sClist_node_t *pst_node);

void clist_init(sClist_t *pst_list)
{
    pst_list->pst_head = prvNodeNew(0);
    pst_list->pst_head->pst_next = pst_list->pst_head;    /* self-loop */
    pst_list->uw_len = 0u;
}

void clist_destroy(sClist_t *pst_list)
{
    sClist_node_t *pst_cur = pst_list->pst_head->pst_next;
    sClist_node_t *pst_tmp;

    while(pst_cur != pst_list->pst_head)
    {
        pst_tmp = pst_cur;
        pst_cur = pst_cur->pst_next;
        prvNodeFree(pst_tmp);
    }
    prvNodeFree(pst_list->pst_head);
    pst_list->pst_head = NULL;
    pst_list->uw_len = 0u;
}

int clist_is_empty(const sClist_t *pst_list)
{
    if(pst_list->pst_head->pst_next == pst_list->pst_head)
    {
        return 1;
    }
    return 0;
}

unsigned int clist_get_len(const sClist_t *pst_list)
{
    return pst_list->uw_len;
}

int clist_get(const sClist_t *pst_list, u32 u32Idx)
{
    sClist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < (unsigned int)u32Idx; uw_i++)
    {
        pst_cur = pst_cur->pst_next;
    }
    return pst_cur->sw_data;
}

/* Insert a value before position u32Idx (0 <= index <= len). */
int clist_insert(sClist_t *pst_list, u32 u32Idx, s32 s32Elem)
{
    sClist_node_t *pst_pre = pst_list->pst_head;
    sClist_node_t *pst_new;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx > pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    pst_new->pst_next = pst_pre->pst_next;
    pst_pre->pst_next = pst_new;
    pst_list->uw_len++;
    return 1;
}

/* Delete the element at position u32Idx and store it via *ps32Elem. */
int clist_delete(sClist_t *pst_list, u32 u32Idx, s32 *ps32Elem)
{
    sClist_node_t *pst_pre = pst_list->pst_head;
    sClist_node_t *pst_del;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_del = pst_pre->pst_next;
    *ps32Elem = pst_del->sw_data;
    pst_pre->pst_next = pst_del->pst_next;
    prvNodeFree(pst_del);
    pst_list->uw_len--;
    return 1;
}

/* Return the index of the first occurrence of s32Elem, or -1 if absent. */
int clist_find(const sClist_t *pst_list, s32 s32Elem)
{
    sClist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i = 0u;

    while(pst_cur != pst_list->pst_head)
    {
        if(pst_cur->sw_data == (int)s32Elem)
        {
            return (int)uw_i;
        }
        pst_cur = pst_cur->pst_next;
        uw_i++;
    }
    return -1;
}

void clist_print(const sClist_t *pst_list)
{
    sClist_node_t *pst_cur = pst_list->pst_head->pst_next;

    while(pst_cur != pst_list->pst_head)
    {
        printf("%d ", pst_cur->sw_data);
        pst_cur = pst_cur->pst_next;
    }
    printf("\n");
}

static sClist_node_t *prvNodeNew(s32 s32Elem)
{
    sClist_node_t *pst_node;

    pst_node = (sClist_node_t *)malloc(sizeof(sClist_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_next = NULL;
    return pst_node;
}

static void prvNodeFree(sClist_node_t *pst_node)
{
    free(pst_node);
}
```

---

## 非循环双链表 dlist

**是什么**：双链表每个结点同时持有**前驱指针**和**后继指针**，因此既能从前向后遍历也能从后向前遍历。相比单链表，插入/删除时对「前驱的更新」更直观，但也多了存储开销。仍带头结点，尾结点后继为 NULL。

**核心操作**：`dlist_init`/`dlist_is_empty`/`dlist_get_len`/`dlist_get`/`dlist_insert`/`dlist_delete`/`dlist_find`/`dlist_print`/`dlist_destroy`。插入时要同步维护后继的 `pst_prev`，删除时同样要断开并接好前后两段。

**结构体字段**：结点 `sDlist_node_t`（`sw_data` + `pst_next` + `pst_prev`），容器 `sDlist_t`。空表时 `head->next == NULL`。

### dlist.h

```c
#ifndef DLIST_H
#define DLIST_H

#include "ds_types.h"

/* Doubly-linked list (non-circular) with a dummy head node.
 * Each node has both a next and a prev pointer, so traversal is possible in
 * both directions. The tail node's next pointer is NULL. */

typedef struct dlist_node
{
    int sw_data;
    struct dlist_node *pst_next;
    struct dlist_node *pst_prev;
} sDlist_node_t;

typedef struct dlist
{
    sDlist_node_t *pst_head;    /* dummy head */
    unsigned int uw_len;
} sDlist_t;

void         dlist_init(sDlist_t *pst_list);
void         dlist_destroy(sDlist_t *pst_list);
int          dlist_is_empty(const sDlist_t *pst_list);
unsigned int dlist_get_len(const sDlist_t *pst_list);
int          dlist_get(const sDlist_t *pst_list, u32 u32Idx);
int          dlist_insert(sDlist_t *pst_list, u32 u32Idx, s32 s32Elem);
int          dlist_delete(sDlist_t *pst_list, u32 u32Idx, s32 *ps32Elem);
int          dlist_find(const sDlist_t *pst_list, s32 s32Elem);
void         dlist_print(const sDlist_t *pst_list);

#endif /* DLIST_H */
```

### dlist.c

```c
/* Doubly-linked list (non-circular) with dummy head node. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: dlist.c
Author: name
version: V1.0.0
function: Doubly-linked list with dummy head - init, length, get, insert,
          delete, find, print, destroy.
*/
/*
Notes:
1. head is a dummy node; head->prev and tail->next are NULL.
2. Indices are zero-based; valid range [0, len).
*/
/*
Changelog:
1. Initial release.
*/

#include "dlist.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sDlist_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sDlist_node_t *pst_node);

void dlist_init(sDlist_t *pst_list)
{
    pst_list->pst_head = prvNodeNew(0);
    pst_list->pst_head->pst_prev = NULL;
    pst_list->uw_len = 0u;
}

void dlist_destroy(sDlist_t *pst_list)
{
    sDlist_node_t *pst_cur = pst_list->pst_head;
    sDlist_node_t *pst_tmp;

    while(NULL != pst_cur)
    {
        pst_tmp = pst_cur;
        pst_cur = pst_cur->pst_next;
        prvNodeFree(pst_tmp);
    }
    pst_list->pst_head = NULL;
    pst_list->uw_len = 0u;
}

int dlist_is_empty(const sDlist_t *pst_list)
{
    if(NULL == pst_list->pst_head->pst_next)
    {
        return 1;
    }
    return 0;
}

unsigned int dlist_get_len(const sDlist_t *pst_list)
{
    return pst_list->uw_len;
}

int dlist_get(const sDlist_t *pst_list, u32 u32Idx)
{
    sDlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < (unsigned int)u32Idx; uw_i++)
    {
        pst_cur = pst_cur->pst_next;
    }
    return pst_cur->sw_data;
}

/* Insert a value before position u32Idx (0 <= index <= len). */
int dlist_insert(sDlist_t *pst_list, u32 u32Idx, s32 s32Elem)
{
    sDlist_node_t *pst_pre = pst_list->pst_head;
    sDlist_node_t *pst_new;
    sDlist_node_t *pst_succ;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx > pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    pst_succ = pst_pre->pst_next;
    pst_new->pst_next = pst_succ;
    pst_new->pst_prev = pst_pre;
    pst_pre->pst_next = pst_new;
    if(NULL != pst_succ)
    {
        pst_succ->pst_prev = pst_new;
    }
    pst_list->uw_len++;
    return 1;
}

/* Delete the element at position u32Idx and store it via *ps32Elem. */
int dlist_delete(sDlist_t *pst_list, u32 u32Idx, s32 *ps32Elem)
{
    sDlist_node_t *pst_del;
    sDlist_node_t *pst_pre;
    sDlist_node_t *pst_succ;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    pst_pre = pst_list->pst_head;
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_del = pst_pre->pst_next;
    *ps32Elem = pst_del->sw_data;
    pst_succ = pst_del->pst_next;
    pst_pre->pst_next = pst_succ;
    if(NULL != pst_succ)
    {
        pst_succ->pst_prev = pst_pre;
    }
    prvNodeFree(pst_del);
    pst_list->uw_len--;
    return 1;
}

/* Return the index of the first occurrence of s32Elem, or -1 if absent. */
int dlist_find(const sDlist_t *pst_list, s32 s32Elem)
{
    sDlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i = 0u;

    while(NULL != pst_cur)
    {
        if(pst_cur->sw_data == (int)s32Elem)
        {
            return (int)uw_i;
        }
        pst_cur = pst_cur->pst_next;
        uw_i++;
    }
    return -1;
}

void dlist_print(const sDlist_t *pst_list)
{
    sDlist_node_t *pst_cur = pst_list->pst_head->pst_next;

    while(NULL != pst_cur)
    {
        printf("%d ", pst_cur->sw_data);
        pst_cur = pst_cur->pst_next;
    }
    printf("\n");
}

static sDlist_node_t *prvNodeNew(s32 s32Elem)
{
    sDlist_node_t *pst_node;

    pst_node = (sDlist_node_t *)malloc(sizeof(sDlist_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_next = NULL;
    pst_node->pst_prev = NULL;
    return pst_node;
}

static void prvNodeFree(sDlist_node_t *pst_node)
{
    free(pst_node);
}
```

---

## 循环双链表 cdlist

**是什么**：循环双链表把双链表首尾相接成环——`head->prev` 指向尾结点，`head->next` 仍指向首结点。结合「双向 + 环形」，从头尾两端都能 O(1) 定位与插入，是四类链表中功能最强的一种。仍带哑元头结点。

**核心操作**：在 `dlist` 基础上增加头插 `cdlist_insert_head`（直接等价于在索引 0 插入）。其余 `cdlist_init`/`cdlist_insert`/`cdlist_delete`/`cdlist_find`/`cdlist_print` 与 `dlist` 对应，但遍历终止条件是回到头结点。

**结构体字段**：结点 `sCdlist_node_t`（`sw_data` + `pst_next` + `pst_prev`），容器 `sCdlist_t`。空表时 `head->next == head` 且 `head->prev == head`（自环双指）。

### cdlist.h

```c
#ifndef CDLIST_H
#define CDLIST_H

#include "ds_types.h"

/* Circular doubly-linked list with a dummy head node.
 * The head node's prev points to the tail and the tail node's next points
 * back to the head, so the whole list is a two-way ring. */

typedef struct cdlist_node
{
    int sw_data;
    struct cdlist_node *pst_next;
    struct cdlist_node *pst_prev;
} sCdlist_node_t;

typedef struct cdlist
{
    sCdlist_node_t *pst_head;    /* dummy head; forms a two-way ring */
    unsigned int uw_len;
} sCdlist_t;

void         cdlist_init(sCdlist_t *pst_list);
void         cdlist_destroy(sCdlist_t *pst_list);
int          cdlist_is_empty(const sCdlist_t *pst_list);
unsigned int cdlist_get_len(const sCdlist_t *pst_list);
int          cdlist_get(const sCdlist_t *pst_list, u32 u32Idx);
int          cdlist_insert(sCdlist_t *pst_list, u32 u32Idx, s32 s32Elem);
int          cdlist_insert_head(sCdlist_t *pst_list, s32 s32Elem);
int          cdlist_delete(sCdlist_t *pst_list, u32 u32Idx, s32 *ps32Elem);
int          cdlist_find(const sCdlist_t *pst_list, s32 s32Elem);
void         cdlist_print(const sCdlist_t *pst_list);

#endif /* CDLIST_H */
```

### cdlist.c

```c
/* Circular doubly-linked list with dummy head node. The head and tail nodes
 * point to each other, so the list is traversable from either end. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: cdlist.c
Author: name
version: V1.0.0
function: Circular doubly-linked list - init, length, get, insert, insert at
          head, delete, find, print, destroy.
*/
/*
Notes:
1. head->prev == tail and tail->next == head, forming a two-way ring.
2. Indices are zero-based; valid range [0, len).
*/
/*
Changelog:
1. Initial release.
*/

#include "cdlist.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sCdlist_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sCdlist_node_t *pst_node);

void cdlist_init(sCdlist_t *pst_list)
{
    pst_list->pst_head = prvNodeNew(0);
    pst_list->pst_head->pst_next = pst_list->pst_head;
    pst_list->pst_head->pst_prev = pst_list->pst_head;
    pst_list->uw_len = 0u;
}

void cdlist_destroy(sCdlist_t *pst_list)
{
    sCdlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    sCdlist_node_t *pst_tmp;

    while(pst_cur != pst_list->pst_head)
    {
        pst_tmp = pst_cur;
        pst_cur = pst_cur->pst_next;
        prvNodeFree(pst_tmp);
    }
    prvNodeFree(pst_list->pst_head);
    pst_list->pst_head = NULL;
    pst_list->uw_len = 0u;
}

int cdlist_is_empty(const sCdlist_t *pst_list)
{
    if(pst_list->pst_head->pst_next == pst_list->pst_head)
    {
        return 1;
    }
    return 0;
}

unsigned int cdlist_get_len(const sCdlist_t *pst_list)
{
    return pst_list->uw_len;
}

int cdlist_get(const sCdlist_t *pst_list, u32 u32Idx)
{
    sCdlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < (unsigned int)u32Idx; uw_i++)
    {
        pst_cur = pst_cur->pst_next;
    }
    return pst_cur->sw_data;
}

/* Insert a value before position u32Idx (0 <= index <= len). */
int cdlist_insert(sCdlist_t *pst_list, u32 u32Idx, s32 s32Elem)
{
    sCdlist_node_t *pst_pre = pst_list->pst_head;
    sCdlist_node_t *pst_new;
    sCdlist_node_t *pst_succ;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx > pst_list->uw_len)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    pst_succ = pst_pre->pst_next;

    pst_new->pst_next = pst_succ;
    pst_new->pst_prev = pst_pre;
    pst_pre->pst_next = pst_new;
    pst_succ->pst_prev = pst_new;

    pst_list->uw_len++;
    return 1;
}

/* Insert at the very head of the list (before the first real element). */
int cdlist_insert_head(sCdlist_t *pst_list, s32 s32Elem)
{
    return cdlist_insert(pst_list, 0u, s32Elem);
}

/* Delete the element at position u32Idx and store it via *ps32Elem. */
int cdlist_delete(sCdlist_t *pst_list, u32 u32Idx, s32 *ps32Elem)
{
    sCdlist_node_t *pst_del;
    sCdlist_node_t *pst_pre;
    unsigned int uw_i;
    unsigned int uw_idx = (unsigned int)u32Idx;

    if(u32Idx >= pst_list->uw_len)
    {
        return 0;
    }
    pst_pre = pst_list->pst_head;
    for(uw_i = 0u; uw_i < uw_idx; uw_i++)
    {
        pst_pre = pst_pre->pst_next;
    }
    pst_del = pst_pre->pst_next;
    *ps32Elem = pst_del->sw_data;

    pst_pre->pst_next = pst_del->pst_next;
    pst_del->pst_next->pst_prev = pst_pre;

    prvNodeFree(pst_del);
    pst_list->uw_len--;
    return 1;
}

/* Return the index of the first occurrence of s32Elem, or -1 if absent. */
int cdlist_find(const sCdlist_t *pst_list, s32 s32Elem)
{
    sCdlist_node_t *pst_cur = pst_list->pst_head->pst_next;
    unsigned int uw_i = 0u;

    while(pst_cur != pst_list->pst_head)
    {
        if(pst_cur->sw_data == (int)s32Elem)
        {
            return (int)uw_i;
        }
        pst_cur = pst_cur->pst_next;
        uw_i++;
    }
    return -1;
}

void cdlist_print(const sCdlist_t *pst_list)
{
    sCdlist_node_t *pst_cur = pst_list->pst_head->pst_next;

    while(pst_cur != pst_list->pst_head)
    {
        printf("%d ", pst_cur->sw_data);
        pst_cur = pst_cur->pst_next;
    }
    printf("\n");
}

static sCdlist_node_t *prvNodeNew(s32 s32Elem)
{
    sCdlist_node_t *pst_node;

    pst_node = (sCdlist_node_t *)malloc(sizeof(sCdlist_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_next = NULL;
    pst_node->pst_prev = NULL;
    return pst_node;
}

static void prvNodeFree(sCdlist_node_t *pst_node)
{
    free(pst_node);
}
```

---

## 顺序栈 seqstack

**是什么**：栈是「后进先出（LIFO）」的线性表，只允许在同一端（栈顶）插入和删除。顺序栈用一段连续数组存元素，`uw_top` 记录元素个数，栈顶元素位于下标 `uw_top-1`。本实现支持动态扩容。

**核心操作**：`seqstack_init`/`seqstack_destroy`/`seqstack_is_empty`/`seqstack_get_len`/`seqstack_push`（入栈）/`seqstack_pop`（出栈）/`seqstack_top`（只读栈顶，不出栈）/`seqstack_print`。当数组满时自动扩容一倍。

**结构体字段**：元素指针 `psw_aData`、栈顶计数 `uw_top`、容量 `uw_cap`。

### seqstack.h

```c
#ifndef SEQSTACK_H
#define SEQSTACK_H

#include "ds_types.h"

/* Sequential stack (array-backed) with dynamic growth.
 * The stack top is at index uw_top; index 0 is the bottom. */

typedef struct seq_stack
{
    int *psw_aData;         /* element buffer, allocated at init */
    unsigned int uw_top;    /* index of the top element, or 0 if empty */
    unsigned int uw_cap;    /* allocated capacity */
} sSeq_stack_t;

void         seqstack_init(sSeq_stack_t *pst_stack, u32 u32Cap);
void         seqstack_destroy(sSeq_stack_t *pst_stack);
int          seqstack_is_empty(const sSeq_stack_t *pst_stack);
unsigned int seqstack_get_len(const sSeq_stack_t *pst_stack);
int          seqstack_push(sSeq_stack_t *pst_stack, s32 s32Elem);
int          seqstack_pop(sSeq_stack_t *pst_stack, s32 *ps32Elem);
int          seqstack_top(const sSeq_stack_t *pst_stack, s32 *ps32Elem);
void         seqstack_print(const sSeq_stack_t *pst_stack);

#endif /* SEQSTACK_H */
```

### seqstack.c

```c
/* Sequential stack (array-backed) with dynamic growth.
 * uw_top counts the number of elements; the top element is at index uw_top-1. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: seqstack.c
Author: name
version: V1.0.0
function: Sequential stack - init, push, pop, top, length, print, destroy.
*/
/*
Notes:
1. Dynamic array owned by the caller; memory is allocated at init.
2. uw_top is the element count; top element lives at index uw_top-1.
*/
/*
Changelog:
1. Initial release.
*/

#include "seqstack.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static int prvEnsureCapacity(sSeq_stack_t *pst_stack);

void seqstack_init(sSeq_stack_t *pst_stack, u32 u32Cap)
{
    pst_stack->psw_aData = (int *)malloc(sizeof(int) * (size_t)u32Cap);
    pst_stack->uw_top = 0u;
    pst_stack->uw_cap = (unsigned int)u32Cap;
}

void seqstack_destroy(sSeq_stack_t *pst_stack)
{
    free(pst_stack->psw_aData);
    pst_stack->psw_aData = NULL;
    pst_stack->uw_top = 0u;
    pst_stack->uw_cap = 0u;
}

int seqstack_is_empty(const sSeq_stack_t *pst_stack)
{
    if(0u == pst_stack->uw_top)
    {
        return 1;
    }
    return 0;
}

unsigned int seqstack_get_len(const sSeq_stack_t *pst_stack)
{
    return pst_stack->uw_top;
}

/* Push a value onto the top of the stack. */
int seqstack_push(sSeq_stack_t *pst_stack, s32 s32Elem)
{
    if(0 == prvEnsureCapacity(pst_stack))
    {
        return 0;
    }
    pst_stack->psw_aData[pst_stack->uw_top] = (int)s32Elem;
    pst_stack->uw_top++;
    return 1;
}

/* Pop the top value and store it via *ps32Elem. */
int seqstack_pop(sSeq_stack_t *pst_stack, s32 *ps32Elem)
{
    if(0u == pst_stack->uw_top)
    {
        return 0;
    }
    pst_stack->uw_top--;
    *ps32Elem = (s32)pst_stack->psw_aData[pst_stack->uw_top];
    return 1;
}

/* Read the top value without removing it; store via *ps32Elem. */
int seqstack_top(const sSeq_stack_t *pst_stack, s32 *ps32Elem)
{
    if(0u == pst_stack->uw_top)
    {
        return 0;
    }
    *ps32Elem = (s32)pst_stack->psw_aData[pst_stack->uw_top - 1u];
    return 1;
}

void seqstack_print(const sSeq_stack_t *pst_stack)
{
    unsigned int uw_i;

    printf("(top) ");
    for(uw_i = pst_stack->uw_top; uw_i > 0u; uw_i--)
    {
        printf("%d ", pst_stack->psw_aData[uw_i - 1u]);
    }
    printf("(bottom)\n");
}

/* Private: grow the buffer when it is full. Returns 1 on success. */
static int prvEnsureCapacity(sSeq_stack_t *pst_stack)
{
    int *psw_tmp;
    unsigned int uw_i;
    unsigned int uw_new_cap;

    if(pst_stack->uw_top < pst_stack->uw_cap)
    {
        return 1;
    }
    uw_new_cap = (0u == pst_stack->uw_cap) ? 4u : (pst_stack->uw_cap * 2u);
    psw_tmp = (int *)malloc(sizeof(int) * (size_t)uw_new_cap);
    if(NULL == psw_tmp)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < pst_stack->uw_top; uw_i++)
    {
        psw_tmp[uw_i] = pst_stack->psw_aData[uw_i];
    }
    free(pst_stack->psw_aData);
    pst_stack->psw_aData = psw_tmp;
    pst_stack->uw_cap = uw_new_cap;
    return 1;
}
```

---

## 链栈 linkstack

**是什么**：链栈用单链表实现栈，**栈顶即链表头结点**。入栈在头插、出栈在头删，因此无论如何都是 O(1)；相比顺序栈无需预分配容量和扩容，天然动态。

**核心操作**：`linkstack_init`/`linkstack_destroy`/`linkstack_is_empty`/`linkstack_get_len`/`linkstack_push`/`linkstack_pop`/`linkstack_top`/`linkstack_print`。空栈时 `pst_top == NULL`。

**结构体字段**：结点 `sLinkstack_node_t`（`sw_data` + `pst_next`），容器 `sLinkstack_t`（栈顶指针 `pst_top` + 计数 `uw_len`）。

### linkstack.h

```c
#ifndef LINKSTACK_H
#define LINKSTACK_H

#include "ds_types.h"

/* Linked stack: the stack top is the first node of a singly-linked chain. */

typedef struct linkstack_node
{
    int sw_data;
    struct linkstack_node *pst_next;
} sLinkstack_node_t;

typedef struct linkstack
{
    sLinkstack_node_t *pst_top;    /* top of the stack, NULL when empty */
    unsigned int uw_len;
} sLinkstack_t;

void         linkstack_init(sLinkstack_t *pst_stack);
void         linkstack_destroy(sLinkstack_t *pst_stack);
int          linkstack_is_empty(const sLinkstack_t *pst_stack);
unsigned int linkstack_get_len(const sLinkstack_t *pst_stack);
int          linkstack_push(sLinkstack_t *pst_stack, s32 s32Elem);
int          linkstack_pop(sLinkstack_t *pst_stack, s32 *ps32Elem);
int          linkstack_top(const sLinkstack_t *pst_stack, s32 *ps32Elem);
void         linkstack_print(const sLinkstack_t *pst_stack);

#endif /* LINKSTACK_H */
```

### linkstack.c

```c
/* Linked stack: push and pop occur at the head of a singly-linked chain. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: linkstack.c
Author: name
version: V1.0.0
function: Linked stack - init, push, pop, top, length, print, destroy.
*/
/*
Notes:
1. The stack top is the first node of the chain (pst_top).
2. An empty stack has pst_top == NULL and uw_len == 0.
*/
/*
Changelog:
1. Initial release.
*/

#include "linkstack.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sLinkstack_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sLinkstack_node_t *pst_node);

void linkstack_init(sLinkstack_t *pst_stack)
{
    pst_stack->pst_top = NULL;
    pst_stack->uw_len = 0u;
}

void linkstack_destroy(sLinkstack_t *pst_stack)
{
    sLinkstack_node_t *pst_cur = pst_stack->pst_top;
    sLinkstack_node_t *pst_tmp;

    while(NULL != pst_cur)
    {
        pst_tmp = pst_cur;
        pst_cur = pst_cur->pst_next;
        prvNodeFree(pst_tmp);
    }
    pst_stack->pst_top = NULL;
    pst_stack->uw_len = 0u;
}

int linkstack_is_empty(const sLinkstack_t *pst_stack)
{
    if(NULL == pst_stack->pst_top)
    {
        return 1;
    }
    return 0;
}

unsigned int linkstack_get_len(const sLinkstack_t *pst_stack)
{
    return pst_stack->uw_len;
}

/* Push a value onto the top of the stack. */
int linkstack_push(sLinkstack_t *pst_stack, s32 s32Elem)
{
    sLinkstack_node_t *pst_new;

    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    pst_new->pst_next = pst_stack->pst_top;
    pst_stack->pst_top = pst_new;
    pst_stack->uw_len++;
    return 1;
}

/* Pop the top value and store it via *ps32Elem. */
int linkstack_pop(sLinkstack_t *pst_stack, s32 *ps32Elem)
{
    sLinkstack_node_t *pst_del;

    if(NULL == pst_stack->pst_top)
    {
        return 0;
    }
    pst_del = pst_stack->pst_top;
    *ps32Elem = pst_del->sw_data;
    pst_stack->pst_top = pst_del->pst_next;
    prvNodeFree(pst_del);
    pst_stack->uw_len--;
    return 1;
}

/* Read the top value without removing it; store via *ps32Elem. */
int linkstack_top(const sLinkstack_t *pst_stack, s32 *ps32Elem)
{
    if(NULL == pst_stack->pst_top)
    {
        return 0;
    }
    *ps32Elem = pst_stack->pst_top->sw_data;
    return 1;
}

void linkstack_print(const sLinkstack_t *pst_stack)
{
    sLinkstack_node_t *pst_cur = pst_stack->pst_top;

    printf("(top) ");
    while(NULL != pst_cur)
    {
        printf("%d ", pst_cur->sw_data);
        pst_cur = pst_cur->pst_next;
    }
    printf("(bottom)\n");
}

static sLinkstack_node_t *prvNodeNew(s32 s32Elem)
{
    sLinkstack_node_t *pst_node;

    pst_node = (sLinkstack_node_t *)malloc(sizeof(sLinkstack_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_next = NULL;
    return pst_node;
}

static void prvNodeFree(sLinkstack_node_t *pst_node)
{
    free(pst_node);
}
```

---

## 循环队列 seqqueue

**是什么**：队列是「先进先出（FIFO）」的线性表，队尾插入、队头删除。顺序队列为复用数组空间采用**循环队列**，`front` 指向队头元素下标、`rear` 指向下一个可插入位置。为了避免「队满与队空」的歧义，本实现额外用一个 `uw_cnt` 记录元素个数，而非浪费一个空槽。

**核心操作**：`seqqueue_init`/`seqqueue_destroy`/`seqqueue_is_empty`/`seqqueue_is_full`/`seqqueue_get_len`/`seqqueue_enqueue`（入队）/`seqqueue_dequeue`（出队）/`seqqueue_front`（读队头，不出队）/`seqqueue_print`。数组满时自动扩容并重新对齐首尾。

**结构体字段**：元素指针 `psw_aData`、队头下标 `uw_front`、队尾下标 `uw_rear`、元素计数 `uw_cnt`、容量 `uw_cap`。

### seqqueue.h

```c
#ifndef SEQQUEUE_H
#define SEQQUEUE_H

#include "ds_types.h"

/* Circular queue (array-backed) with dynamic growth.
 * front is the index of the first element, rear is the next insertion slot.
 * A dedicated runtime count (uw_cnt) distinguishes full from empty. */

typedef struct seq_queue
{
    int *psw_aData;          /* element buffer */
    unsigned int uw_front;   /* index of the front element */
    unsigned int uw_rear;    /* index of the next insertion slot */
    unsigned int uw_cnt;     /* current element count */
    unsigned int uw_cap;     /* allocated capacity */
} sSeq_queue_t;

void         seqqueue_init(sSeq_queue_t *pst_queue, u32 u32Cap);
void         seqqueue_destroy(sSeq_queue_t *pst_queue);
int          seqqueue_is_empty(const sSeq_queue_t *pst_queue);
int          seqqueue_is_full(const sSeq_queue_t *pst_queue);
unsigned int seqqueue_get_len(const sSeq_queue_t *pst_queue);
int          seqqueue_enqueue(sSeq_queue_t *pst_queue, s32 s32Elem);
int          seqqueue_dequeue(sSeq_queue_t *pst_queue, s32 *ps32Elem);
int          seqqueue_front(const sSeq_queue_t *pst_queue, s32 *ps32Elem);
void         seqqueue_print(const sSeq_queue_t *pst_queue);

#endif /* SEQQUEUE_H */
```

### seqqueue.c

```c
/* Circular queue (array-backed) with dynamic growth. A count field is kept
 * to distinguish the full state from the empty state without a wasted slot. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: seqqueue.c
Author: name
version: V1.0.0
function: Circular queue - init, enqueue, dequeue, front, length, full check,
          print, destroy.
*/
/*
Notes:
1. uw_front is the first element; uw_rear is the next insertion slot.
2. uw_cnt tracks the live count so full and empty are distinct.
*/
/*
Changelog:
1. Initial release.
*/

#include "seqqueue.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static int prvEnsureCapacity(sSeq_queue_t *pst_queue);

void seqqueue_init(sSeq_queue_t *pst_queue, u32 u32Cap)
{
    pst_queue->psw_aData = (int *)malloc(sizeof(int) * (size_t)u32Cap);
    pst_queue->uw_front = 0u;
    pst_queue->uw_rear = 0u;
    pst_queue->uw_cnt = 0u;
    pst_queue->uw_cap = (unsigned int)u32Cap;
}

void seqqueue_destroy(sSeq_queue_t *pst_queue)
{
    free(pst_queue->psw_aData);
    pst_queue->psw_aData = NULL;
    pst_queue->uw_front = 0u;
    pst_queue->uw_rear = 0u;
    pst_queue->uw_cnt = 0u;
    pst_queue->uw_cap = 0u;
}

int seqqueue_is_empty(const sSeq_queue_t *pst_queue)
{
    if(0u == pst_queue->uw_cnt)
    {
        return 1;
    }
    return 0;
}

int seqqueue_is_full(const sSeq_queue_t *pst_queue)
{
    if(pst_queue->uw_cnt == pst_queue->uw_cap)
    {
        return 1;
    }
    return 0;
}

unsigned int seqqueue_get_len(const sSeq_queue_t *pst_queue)
{
    return pst_queue->uw_cnt;
}

/* Append a value to the rear of the queue. */
int seqqueue_enqueue(sSeq_queue_t *pst_queue, s32 s32Elem)
{
    if(0 == prvEnsureCapacity(pst_queue))
    {
        return 0;
    }
    pst_queue->psw_aData[pst_queue->uw_rear] = (int)s32Elem;
    pst_queue->uw_rear = (pst_queue->uw_rear + 1u) % pst_queue->uw_cap;
    pst_queue->uw_cnt++;
    return 1;
}

/* Remove the front value and store it via *ps32Elem. */
int seqqueue_dequeue(sSeq_queue_t *pst_queue, s32 *ps32Elem)
{
    if(0u == pst_queue->uw_cnt)
    {
        return 0;
    }
    *ps32Elem = (s32)pst_queue->psw_aData[pst_queue->uw_front];
    pst_queue->uw_front = (pst_queue->uw_front + 1u) % pst_queue->uw_cap;
    pst_queue->uw_cnt--;
    return 1;
}

/* Read the front value without removing it; store via *ps32Elem. */
int seqqueue_front(const sSeq_queue_t *pst_queue, s32 *ps32Elem)
{
    if(0u == pst_queue->uw_cnt)
    {
        return 0;
    }
    *ps32Elem = (s32)pst_queue->psw_aData[pst_queue->uw_front];
    return 1;
}

void seqqueue_print(const sSeq_queue_t *pst_queue)
{
    unsigned int uw_i;
    unsigned int uw_pos;

    printf("(front) ");
    for(uw_i = 0u; uw_i < pst_queue->uw_cnt; uw_i++)
    {
        uw_pos = (pst_queue->uw_front + uw_i) % pst_queue->uw_cap;
        printf("%d ", pst_queue->psw_aData[uw_pos]);
    }
    printf("(rear)\n");
}

/* Private: grow the ring buffer when it is full. Returns 1 on success. */
static int prvEnsureCapacity(sSeq_queue_t *pst_queue)
{
    int *psw_tmp;
    unsigned int uw_i;
    unsigned int uw_pos;
    unsigned int uw_new_cap;

    if(pst_queue->uw_cnt < pst_queue->uw_cap)
    {
        return 1;
    }
    uw_new_cap = (0u == pst_queue->uw_cap) ? 4u : (pst_queue->uw_cap * 2u);
    psw_tmp = (int *)malloc(sizeof(int) * (size_t)uw_new_cap);
    if(NULL == psw_tmp)
    {
        return 0;
    }
    for(uw_i = 0u; uw_i < pst_queue->uw_cnt; uw_i++)
    {
        uw_pos = (pst_queue->uw_front + uw_i) % pst_queue->uw_cap;
        psw_tmp[uw_i] = pst_queue->psw_aData[uw_pos];
    }
    free(pst_queue->psw_aData);
    pst_queue->psw_aData = psw_tmp;
    pst_queue->uw_front = 0u;
    pst_queue->uw_rear = pst_queue->uw_cnt;
    pst_queue->uw_cap = uw_new_cap;
    return 1;
}
```

---

## 链队 linkqueue

**是什么**：链队用单链表实现队列，队头即链表头、队尾即链表尾，并分别用 `pst_front`/`pst_rear` 两个指针指向。入队追加到队尾、出队移走队头，均 O(1)，且无需预分配容量。

**核心操作**：`linkqueue_init`/`linkqueue_destroy`/`linkqueue_is_empty`/`linkqueue_get_len`/`linkqueue_enqueue`/`linkqueue_dequeue`/`linkqueue_front`/`linkqueue_print`。空队时两个指针均为 NULL。

**结构体字段**：结点 `sLinkqueue_node_t`（`sw_data` + `pst_next`），容器 `sLinkqueue_t`（队头 `pst_front` + 队尾 `pst_rear` + 计数 `uw_len`）。

### linkqueue.h

```c
#ifndef LINKQUEUE_H
#define LINKQUEUE_H

#include "ds_types.h"

/* Linked queue: enqueue at the tail, dequeue at the head. */

typedef struct linkqueue_node
{
    int sw_data;
    struct linkqueue_node *pst_next;
} sLinkqueue_node_t;

typedef struct linkqueue
{
    sLinkqueue_node_t *pst_front;    /* head of the queue, NULL when empty */
    sLinkqueue_node_t *pst_rear;     /* tail of the queue */
    unsigned int uw_len;
} sLinkqueue_t;

void         linkqueue_init(sLinkqueue_t *pst_queue);
void         linkqueue_destroy(sLinkqueue_t *pst_queue);
int          linkqueue_is_empty(const sLinkqueue_t *pst_queue);
unsigned int linkqueue_get_len(const sLinkqueue_t *pst_queue);
int          linkqueue_enqueue(sLinkqueue_t *pst_queue, s32 s32Elem);
int          linkqueue_dequeue(sLinkqueue_t *pst_queue, s32 *ps32Elem);
int          linkqueue_front(const sLinkqueue_t *pst_queue, s32 *ps32Elem);
void         linkqueue_print(const sLinkqueue_t *pst_queue);

#endif /* LINKQUEUE_H */
```

### linkqueue.c

```c
/* Linked queue: a singly-linked chain with separate front and rear pointers.
 * Enqueue appends at the rear, dequeue removes at the front. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: linkqueue.c
Author: name
version: V1.0.0
function: Linked queue - init, enqueue, dequeue, front, length, print,
          destroy.
*/
/*
Notes:
1. pst_front is the first node; pst_rear is the last node.
2. An empty queue has both pointers NULL and uw_len == 0.
*/
/*
Changelog:
1. Initial release.
*/

#include "linkqueue.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sLinkqueue_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sLinkqueue_node_t *pst_node);

void linkqueue_init(sLinkqueue_t *pst_queue)
{
    pst_queue->pst_front = NULL;
    pst_queue->pst_rear = NULL;
    pst_queue->uw_len = 0u;
}

void linkqueue_destroy(sLinkqueue_t *pst_queue)
{
    sLinkqueue_node_t *pst_cur = pst_queue->pst_front;
    sLinkqueue_node_t *pst_tmp;

    while(NULL != pst_cur)
    {
        pst_tmp = pst_cur;
        pst_cur = pst_cur->pst_next;
        prvNodeFree(pst_tmp);
    }
    pst_queue->pst_front = NULL;
    pst_queue->pst_rear = NULL;
    pst_queue->uw_len = 0u;
}

int linkqueue_is_empty(const sLinkqueue_t *pst_queue)
{
    if(NULL == pst_queue->pst_front)
    {
        return 1;
    }
    return 0;
}

unsigned int linkqueue_get_len(const sLinkqueue_t *pst_queue)
{
    return pst_queue->uw_len;
}

/* Append a value to the rear of the queue. */
int linkqueue_enqueue(sLinkqueue_t *pst_queue, s32 s32Elem)
{
    sLinkqueue_node_t *pst_new;

    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    if(NULL == pst_queue->pst_rear)
    {
        pst_queue->pst_front = pst_new;
        pst_queue->pst_rear = pst_new;
    }
    else
    {
        pst_queue->pst_rear->pst_next = pst_new;
        pst_queue->pst_rear = pst_new;
    }
    pst_queue->uw_len++;
    return 1;
}

/* Remove the front value and store it via *ps32Elem. */
int linkqueue_dequeue(sLinkqueue_t *pst_queue, s32 *ps32Elem)
{
    sLinkqueue_node_t *pst_del;

    if(NULL == pst_queue->pst_front)
    {
        return 0;
    }
    pst_del = pst_queue->pst_front;
    *ps32Elem = pst_del->sw_data;
    pst_queue->pst_front = pst_del->pst_next;
    if(NULL == pst_queue->pst_front)
    {
        pst_queue->pst_rear = NULL;
    }
    prvNodeFree(pst_del);
    pst_queue->uw_len--;
    return 1;
}

/* Read the front value without removing it; store via *ps32Elem. */
int linkqueue_front(const sLinkqueue_t *pst_queue, s32 *ps32Elem)
{
    if(NULL == pst_queue->pst_front)
    {
        return 0;
    }
    *ps32Elem = pst_queue->pst_front->sw_data;
    return 1;
}

void linkqueue_print(const sLinkqueue_t *pst_queue)
{
    sLinkqueue_node_t *pst_cur = pst_queue->pst_front;

    printf("(front) ");
    while(NULL != pst_cur)
    {
        printf("%d ", pst_cur->sw_data);
        pst_cur = pst_cur->pst_next;
    }
    printf("(rear)\n");
}

static sLinkqueue_node_t *prvNodeNew(s32 s32Elem)
{
    sLinkqueue_node_t *pst_node;

    pst_node = (sLinkqueue_node_t *)malloc(sizeof(sLinkqueue_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_next = NULL;
    return pst_node;
}

static void prvNodeFree(sLinkqueue_node_t *pst_node)
{
    free(pst_node);
}
```

---

## 二叉树 btree

**是什么**：二叉树是每个结点最多有两个子树（左、右）的树结构。本实现采用**二叉链表**（child 指针）存储。这里建树接口是「指定父结点 + 插入左/右孩子」，方便按题目手工构造一棵确定的树，再演示先序/中序/后序/层序四种遍历。

**核心操作**：`btree_init`/`btree_destroy`/`btree_is_empty`/`btree_get_cnt`/`btree_insert_root`（建根）/`btree_insert_left`/`btree_insert_right`（挂左右孩子）/`btree_preorder`/`btree_inorder`/`btree_postorder`/`btree_levelorder`。四种遍历结果与树形一一对应，层序遍历借助一个数组环形队列实现 BFS。

**结构体字段**：结点 `sBtree_node_t`（`sw_data` + `pst_left` + `pst_right`），容器 `sBtree_t`（根 `pst_root` + 计数 `uw_cnt`）。

### btree.h

```c
#ifndef BTREE_H
#define BTREE_H

#include "ds_types.h"

/* Binary tree stored with child pointers (binary linked representation).
 * Node values are integers; an empty tree has a NULL root. */

typedef struct btree_node
{
    int sw_data;
    struct btree_node *pst_left;
    struct btree_node *pst_right;
} sBtree_node_t;

typedef struct btree
{
    sBtree_node_t *pst_root;    /* root node, NULL when the tree is empty */
    unsigned int uw_cnt;        /* number of nodes */
} sBtree_t;

void         btree_init(sBtree_t *pst_tree);
void         btree_destroy(sBtree_t *pst_tree);
int          btree_is_empty(const sBtree_t *pst_tree);
unsigned int btree_get_cnt(const sBtree_t *pst_tree);
int          btree_insert_root(sBtree_t *pst_tree, s32 s32Elem);
int          btree_insert_left(sBtree_t *pst_tree, u32 u32Parent, s32 s32Elem);
int          btree_insert_right(sBtree_t *pst_tree, u32 u32Parent, s32 s32Elem);
void         btree_preorder(const sBtree_t *pst_tree);
void         btree_inorder(const sBtree_t *pst_tree);
void         btree_postorder(const sBtree_t *pst_tree);
void         btree_levelorder(const sBtree_t *pst_tree);

#endif /* BTREE_H */
```

### btree.c

```c
/* Binary tree (binary linked representation). Traversals are recursive; the
 * level-order traversal uses a simple array ring as a working queue. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: btree.c
Author: name
version: V1.0.0
function: Binary tree - init, insert root/left/right, four traversals, count,
          destroy.
*/
/*
Notes:
1. Insertion by value: prvSearch finds the first node holding u32Parent.
2. Traversals are recursive; level-order uses an internal ring queue.
*/
/*
Changelog:
1. Initial release.
*/

#include "btree.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sBtree_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sBtree_node_t *pst_node);
static void prvTreeFree(sBtree_node_t *pst_node);
static sBtree_node_t *prvSearch(sBtree_node_t *pst_node, int sw_val);
static void prvPreorder(sBtree_node_t *pst_node);
static void prvInorder(sBtree_node_t *pst_node);
static void prvPostorder(sBtree_node_t *pst_node);

void btree_init(sBtree_t *pst_tree)
{
    pst_tree->pst_root = NULL;
    pst_tree->uw_cnt = 0u;
}

void btree_destroy(sBtree_t *pst_tree)
{
    prvTreeFree(pst_tree->pst_root);
    pst_tree->pst_root = NULL;
    pst_tree->uw_cnt = 0u;
}

int btree_is_empty(const sBtree_t *pst_tree)
{
    if(NULL == pst_tree->pst_root)
    {
        return 1;
    }
    return 0;
}

unsigned int btree_get_cnt(const sBtree_t *pst_tree)
{
    return pst_tree->uw_cnt;
}

/* Create the root node if the tree is empty. */
int btree_insert_root(sBtree_t *pst_tree, s32 s32Elem)
{
    sBtree_node_t *pst_root;

    if(NULL != pst_tree->pst_root)
    {
        return 0;
    }
    pst_root = prvNodeNew(s32Elem);
    if(NULL == pst_root)
    {
        return 0;
    }
    pst_tree->pst_root = pst_root;
    pst_tree->uw_cnt++;
    return 1;
}

/* Insert a left child under the first node holding value u32Parent. */
int btree_insert_left(sBtree_t *pst_tree, u32 u32Parent, s32 s32Elem)
{
    sBtree_node_t *pst_parent = prvSearch(pst_tree->pst_root, (int)u32Parent);
    sBtree_node_t *pst_child;

    if(NULL == pst_parent || NULL != pst_parent->pst_left)
    {
        return 0;
    }
    pst_child = prvNodeNew(s32Elem);
    if(NULL == pst_child)
    {
        return 0;
    }
    pst_parent->pst_left = pst_child;
    pst_tree->uw_cnt++;
    return 1;
}

/* Insert a right child under the first node holding value u32Parent. */
int btree_insert_right(sBtree_t *pst_tree, u32 u32Parent, s32 s32Elem)
{
    sBtree_node_t *pst_parent = prvSearch(pst_tree->pst_root, (int)u32Parent);
    sBtree_node_t *pst_child;

    if(NULL == pst_parent || NULL != pst_parent->pst_right)
    {
        return 0;
    }
    pst_child = prvNodeNew(s32Elem);
    if(NULL == pst_child)
    {
        return 0;
    }
    pst_parent->pst_right = pst_child;
    pst_tree->uw_cnt++;
    return 1;
}

void btree_preorder(const sBtree_t *pst_tree)
{
    prvPreorder(pst_tree->pst_root);
    printf("\n");
}

void btree_inorder(const sBtree_t *pst_tree)
{
    prvInorder(pst_tree->pst_root);
    printf("\n");
}

void btree_postorder(const sBtree_t *pst_tree)
{
    prvPostorder(pst_tree->pst_root);
    printf("\n");
}

/* Breadth-first level-order traversal using an internal ring queue. */
void btree_levelorder(const sBtree_t *pst_tree)
{
    sBtree_node_t *pst_level[128];
    unsigned int uw_head = 0u;
    unsigned int uw_tail = 0u;
    sBtree_node_t *pst_cur;

    if(NULL == pst_tree->pst_root)
    {
        return;
    }
    pst_level[uw_tail] = pst_tree->pst_root;
    uw_tail = (uw_tail + 1u) % 128u;

    while(uw_head != uw_tail)
    {
        pst_cur = pst_level[uw_head];
        uw_head = (uw_head + 1u) % 128u;
        printf("%d ", pst_cur->sw_data);
        if(NULL != pst_cur->pst_left)
        {
            pst_level[uw_tail] = pst_cur->pst_left;
            uw_tail = (uw_tail + 1u) % 128u;
        }
        if(NULL != pst_cur->pst_right)
        {
            pst_level[uw_tail] = pst_cur->pst_right;
            uw_tail = (uw_tail + 1u) % 128u;
        }
    }
    printf("\n");
}

static sBtree_node_t *prvNodeNew(s32 s32Elem)
{
    sBtree_node_t *pst_node;

    pst_node = (sBtree_node_t *)malloc(sizeof(sBtree_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_left = NULL;
    pst_node->pst_right = NULL;
    return pst_node;
}

static void prvNodeFree(sBtree_node_t *pst_node)
{
    free(pst_node);
}

static void prvTreeFree(sBtree_node_t *pst_node)
{
    if(NULL == pst_node)
    {
        return;
    }
    prvTreeFree(pst_node->pst_left);
    prvTreeFree(pst_node->pst_right);
    prvNodeFree(pst_node);
}

static sBtree_node_t *prvSearch(sBtree_node_t *pst_node, int sw_val)
{
    sBtree_node_t *pst_found;

    if(NULL == pst_node)
    {
        return NULL;
    }
    if(pst_node->sw_data == sw_val)
    {
        return pst_node;
    }
    pst_found = prvSearch(pst_node->pst_left, sw_val);
    if(NULL != pst_found)
    {
        return pst_found;
    }
    return prvSearch(pst_node->pst_right, sw_val);
}

static void prvPreorder(sBtree_node_t *pst_node)
{
    if(NULL == pst_node)
    {
        return;
    }
    printf("%d ", pst_node->sw_data);
    prvPreorder(pst_node->pst_left);
    prvPreorder(pst_node->pst_right);
}

static void prvInorder(sBtree_node_t *pst_node)
{
    if(NULL == pst_node)
    {
        return;
    }
    prvInorder(pst_node->pst_left);
    printf("%d ", pst_node->sw_data);
    prvInorder(pst_node->pst_right);
}

static void prvPostorder(sBtree_node_t *pst_node)
{
    if(NULL == pst_node)
    {
        return;
    }
    prvPostorder(pst_node->pst_left);
    prvPostorder(pst_node->pst_right);
    printf("%d ", pst_node->sw_data);
}
```

---

## 二叉排序树 bst

**是什么**：二叉排序树（二叉搜索树，BST）满足「左子树所有值 < 根 < 右子树所有值」，因此**中序遍历必为升序**。查找、插入都按与根比较决定走左还是走右，均摊 O(log n)；删除分「无孩子 / 一个孩子 / 两个孩子（用中序后继顶替）」三种情况。

**核心操作**：`bst_init`/`bst_destroy`/`bst_is_empty`/`bst_get_cnt`/`bst_insert`/`bst_find`/`bst_delete`/`bst_inorder`。重复值不插入；删除两个孩子结点时用「中序后继」替换。

**结构体字段**：结点 `sBst_node_t`（`sw_data` + `pst_left` + `pst_right`），容器 `sBst_t`（根 `pst_root` + 计数 `uw_cnt`）。注意普通 BST **不自动平衡**，最坏会退化成链；要保证性能需用平衡二叉树（教材只要求手算模拟）。

### bst.h

```c
#ifndef BST_H
#define BST_H

#include "ds_types.h"

/* Binary search tree (BST): every left subtree holds smaller values and every
 * right subtree holds larger values, so in-order traversal is ascending. */

typedef struct bst_node
{
    int sw_data;
    struct bst_node *pst_left;
    struct bst_node *pst_right;
} sBst_node_t;

typedef struct bst
{
    sBst_node_t *pst_root;    /* root node, NULL when the tree is empty */
    unsigned int uw_cnt;
} sBst_t;

void         bst_init(sBst_t *pst_tree);
void         bst_destroy(sBst_t *pst_tree);
int          bst_is_empty(const sBst_t *pst_tree);
unsigned int bst_get_cnt(const sBst_t *pst_tree);
int          bst_insert(sBst_t *pst_tree, s32 s32Elem);
int          bst_find(const sBst_t *pst_tree, s32 s32Elem);
int          bst_delete(sBst_t *pst_tree, s32 s32Elem);
void         bst_inorder(const sBst_t *pst_tree);

#endif /* BST_H */
```

### bst.c

```c
/* Binary search tree. Insert and find are iterative; delete uses the
 * successor-replacement strategy, then rebalances by none (a plain BST). */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: bst.c
Author: name
version: V1.0.0
function: Binary search tree - insert, find, delete, in-order, count, destroy.
*/
/*
Notes:
1. Duplicate values are not inserted.
2. Delete replaces the node with its in-order successor when it has two
   children; a plain BST does not self-balance.
*/
/*
Changelog:
1. Initial release.
*/

#include "bst.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static sBst_node_t *prvNodeNew(s32 s32Elem);
static void prvNodeFree(sBst_node_t *pst_node);
static void prvTreeFree(sBst_node_t *pst_node);
static sBst_node_t *prvFind(const sBst_node_t *pst_node, int sw_val);
static void prvInorder(sBst_node_t *pst_node);

void bst_init(sBst_t *pst_tree)
{
    pst_tree->pst_root = NULL;
    pst_tree->uw_cnt = 0u;
}

void bst_destroy(sBst_t *pst_tree)
{
    prvTreeFree(pst_tree->pst_root);
    pst_tree->pst_root = NULL;
    pst_tree->uw_cnt = 0u;
}

int bst_is_empty(const sBst_t *pst_tree)
{
    if(NULL == pst_tree->pst_root)
    {
        return 1;
    }
    return 0;
}

unsigned int bst_get_cnt(const sBst_t *pst_tree)
{
    return pst_tree->uw_cnt;
}

/* Insert a value, keeping the BST ordering. Returns 0 if it already exists. */
int bst_insert(sBst_t *pst_tree, s32 s32Elem)
{
    sBst_node_t *pst_cur = pst_tree->pst_root;
    sBst_node_t *pst_pre = NULL;
    sBst_node_t *pst_new;
    int sw_val = (int)s32Elem;

    if(NULL == pst_cur)
    {
        pst_new = prvNodeNew(s32Elem);
        if(NULL == pst_new)
        {
            return 0;
        }
        pst_tree->pst_root = pst_new;
        pst_tree->uw_cnt++;
        return 1;
    }

    while(NULL != pst_cur)
    {
        if(sw_val == pst_cur->sw_data)
        {
            return 0;    /* duplicate: reject */
        }
        pst_pre = pst_cur;
        if(sw_val < pst_cur->sw_data)
        {
            pst_cur = pst_cur->pst_left;
        }
        else
        {
            pst_cur = pst_cur->pst_right;
        }
    }

    pst_new = prvNodeNew(s32Elem);
    if(NULL == pst_new)
    {
        return 0;
    }
    if(sw_val < pst_pre->sw_data)
    {
        pst_pre->pst_left = pst_new;
    }
    else
    {
        pst_pre->pst_right = pst_new;
    }
    pst_tree->uw_cnt++;
    return 1;
}

int bst_find(const sBst_t *pst_tree, s32 s32Elem)
{
    if(NULL != prvFind(pst_tree->pst_root, (int)s32Elem))
    {
        return 1;
    }
    return 0;
}

int bst_delete(sBst_t *pst_tree, s32 s32Elem)
{
    sBst_node_t *pst_cur = pst_tree->pst_root;
    sBst_node_t *pst_pre = NULL;
    sBst_node_t *pst_succ;
    sBst_node_t *pst_succ_pre;
    int sw_val = (int)s32Elem;

    while(NULL != pst_cur && sw_val != pst_cur->sw_data)
    {
        pst_pre = pst_cur;
        if(sw_val < pst_cur->sw_data)
        {
            pst_cur = pst_cur->pst_left;
        }
        else
        {
            pst_cur = pst_cur->pst_right;
        }
    }
    if(NULL == pst_cur)
    {
        return 0;    /* not found */
    }

    if(NULL != pst_cur->pst_left && NULL != pst_cur->pst_right)
    {
        /* Two children: replace with the in-order successor. */
        pst_succ_pre = pst_cur;
        pst_succ = pst_cur->pst_right;
        while(NULL != pst_succ->pst_left)
        {
            pst_succ_pre = pst_succ;
            pst_succ = pst_succ->pst_left;
        }
        pst_cur->sw_data = pst_succ->sw_data;
        if(pst_succ_pre->pst_left == pst_succ)
        {
            pst_succ_pre->pst_left = pst_succ->pst_right;
        }
        else
        {
            pst_succ_pre->pst_right = pst_succ->pst_right;
        }
        prvNodeFree(pst_succ);
    }
    else
    {
        /* Zero or one child: splice out the node. */
        pst_succ = (NULL != pst_cur->pst_left) ? pst_cur->pst_left : pst_cur->pst_right;
        if(NULL == pst_pre)
        {
            pst_tree->pst_root = pst_succ;
        }
        else if(pst_pre->pst_left == pst_cur)
        {
            pst_pre->pst_left = pst_succ;
        }
        else
        {
            pst_pre->pst_right = pst_succ;
        }
        prvNodeFree(pst_cur);
    }
    pst_tree->uw_cnt--;
    return 1;
}

void bst_inorder(const sBst_t *pst_tree)
{
    prvInorder(pst_tree->pst_root);
    printf("\n");
}

static sBst_node_t *prvNodeNew(s32 s32Elem)
{
    sBst_node_t *pst_node;

    pst_node = (sBst_node_t *)malloc(sizeof(sBst_node_t));
    if(NULL == pst_node)
    {
        return NULL;
    }
    pst_node->sw_data = (int)s32Elem;
    pst_node->pst_left = NULL;
    pst_node->pst_right = NULL;
    return pst_node;
}

static void prvNodeFree(sBst_node_t *pst_node)
{
    free(pst_node);
}

static void prvTreeFree(sBst_node_t *pst_node)
{
    if(NULL == pst_node)
    {
        return;
    }
    prvTreeFree(pst_node->pst_left);
    prvTreeFree(pst_node->pst_right);
    prvNodeFree(pst_node);
}

static sBst_node_t *prvFind(const sBst_node_t *pst_node, int sw_val)
{
    while(NULL != pst_node)
    {
        if(sw_val == pst_node->sw_data)
        {
            return (sBst_node_t *)pst_node;
        }
        if(sw_val < pst_node->sw_data)
        {
            pst_node = pst_node->pst_left;
        }
        else
        {
            pst_node = pst_node->pst_right;
        }
    }
    return NULL;
}

static void prvInorder(sBst_node_t *pst_node)
{
    if(NULL == pst_node)
    {
        return;
    }
    prvInorder(pst_node->pst_left);
    printf("%d ", pst_node->sw_data);
    prvInorder(pst_node->pst_right);
}
```

---

## 堆 heap

**是什么**：堆是一棵满足「对任意结点，父 >=（或 <=）所有子孙」的**完全二叉树**，本实现为**大顶堆**（根最大），且用数组以 **1-based 索引** 存储：下标 i 的左右孩子为 2i、2i+1。入堆上浮、出堆下沉，建堆用自底向上的下沉。

**核心操作**：`heap_init`/`heap_destroy`/`heap_is_empty`/`heap_get_len`/`heap_push`（上浮）/`heap_pop`（取最大并下沉）/`heap_top`（只读堆顶）/`heap_heapify`（数组建堆）/`heap_print`。

**结构体字段**：元素指针 `psw_aData`（下标 0 保留不用）、长度 `uw_len`、容量 `uw_cap`。堆排序即「反复取堆顶 + 重建堆」，见 `sort_heap`。

### heap.h

```c
#ifndef HEAP_H
#define HEAP_H

#include "ds_types.h"

/* Max-heap stored in a flat array (1-based indexing).
 * The largest element is always at index 1. */

typedef struct heap
{
    int *psw_aData;         /* element buffer, index 0 reserved */
    unsigned int uw_len;    /* current element count */
    unsigned int uw_cap;    /* allocated capacity */
} sHeap_t;

void         heap_init(sHeap_t *pst_heap, u32 u32Cap);
void         heap_destroy(sHeap_t *pst_heap);
int          heap_is_empty(const sHeap_t *pst_heap);
unsigned int heap_get_len(const sHeap_t *pst_heap);
int          heap_push(sHeap_t *pst_heap, s32 s32Elem);
int          heap_pop(sHeap_t *pst_heap, s32 *ps32Elem);
int          heap_top(const sHeap_t *pst_heap, s32 *ps32Elem);
void         heap_heapify(sHeap_t *pst_heap);
void         heap_print(const sHeap_t *pst_heap);

#endif /* HEAP_H */
```

### heap.c

```c
/* Max-heap stored in a flat array with 1-based indexing. Index 0 is unused,
 * so for a node at index i its children live at 2*i and 2*i+1. */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: heap.c
Author: name
version: V1.0.0
function: Max-heap - push, pop, top, heapify, length, print, destroy.
*/
/*
Notes:
1. 1-based indexing: buffer[0] is unused; children of i are 2*i and 2*i+1.
2. push sifts up, pop sifts down from the root, heapify builds in place.
*/
/*
Changelog:
1. Initial release.
*/

#include "heap.h"
#include <stdlib.h>
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static int prvEnsureCapacity(sHeap_t *pst_heap);
static void prvSwap(int *psw_a, int *psw_b);
static void prvSiftUp(sHeap_t *pst_heap);
static void prvSiftDown(sHeap_t *pst_heap, unsigned int uw_start, unsigned int uw_end);

void heap_init(sHeap_t *pst_heap, u32 u32Cap)
{
    unsigned int uw_cap = (unsigned int)u32Cap;
    if(0u == uw_cap)
    {
        uw_cap = 4u;
    }
    pst_heap->psw_aData = (int *)malloc(sizeof(int) * (uw_cap + 1u));
    pst_heap->uw_len = 0u;
    pst_heap->uw_cap = uw_cap;
}

void heap_destroy(sHeap_t *pst_heap)
{
    free(pst_heap->psw_aData);
    pst_heap->psw_aData = NULL;
    pst_heap->uw_len = 0u;
    pst_heap->uw_cap = 0u;
}

int heap_is_empty(const sHeap_t *pst_heap)
{
    if(0u == pst_heap->uw_len)
    {
        return 1;
    }
    return 0;
}

unsigned int heap_get_len(const sHeap_t *pst_heap)
{
    return pst_heap->uw_len;
}

/* Insert a value and sift it up to keep the max-heap property. */
int heap_push(sHeap_t *pst_heap, s32 s32Elem)
{
    if(0 == prvEnsureCapacity(pst_heap))
    {
        return 0;
    }
    pst_heap->uw_len++;
    pst_heap->psw_aData[pst_heap->uw_len] = (int)s32Elem;
    prvSiftUp(pst_heap);
    return 1;
}

/* Remove the maximum (root) and store it via *ps32Elem. */
int heap_pop(sHeap_t *pst_heap, s32 *ps32Elem)
{
    unsigned int uw_last;

    if(0u == pst_heap->uw_len)
    {
        return 0;
    }
    *ps32Elem = (s32)pst_heap->psw_aData[1];
    uw_last = pst_heap->uw_len;
    pst_heap->psw_aData[1] = pst_heap->psw_aData[uw_last];
    pst_heap->uw_len--;
    if(pst_heap->uw_len > 1u)
    {
        prvSiftDown(pst_heap, 1u, pst_heap->uw_len);
    }
    return 1;
}

/* Read the maximum without removing it; store via *ps32Elem. */
int heap_top(const sHeap_t *pst_heap, s32 *ps32Elem)
{
    if(0u == pst_heap->uw_len)
    {
        return 0;
    }
    *ps32Elem = (s32)pst_heap->psw_aData[1];
    return 1;
}

/* Turn the current array into a valid max-heap (bottom-up build). */
void heap_heapify(sHeap_t *pst_heap)
{
    unsigned int uw_start;

    if(pst_heap->uw_len < 2u)
    {
        return;
    }
    uw_start = pst_heap->uw_len / 2u;
    while(uw_start >= 1u)
    {
        prvSiftDown(pst_heap, uw_start, pst_heap->uw_len);
        uw_start--;
    }
}

void heap_print(const sHeap_t *pst_heap)
{
    unsigned int uw_i;

    for(uw_i = 1u; uw_i <= pst_heap->uw_len; uw_i++)
    {
        printf("%d ", pst_heap->psw_aData[uw_i]);
    }
    printf("\n");
}

/* Private: grow the buffer when it is full. Returns 1 on success. */
static int prvEnsureCapacity(sHeap_t *pst_heap)
{
    int *psw_tmp;
    unsigned int uw_i;
    unsigned int uw_new_cap;

    if(pst_heap->uw_len < pst_heap->uw_cap)
    {
        return 1;
    }
    uw_new_cap = pst_heap->uw_cap * 2u;
    psw_tmp = (int *)malloc(sizeof(int) * (uw_new_cap + 1u));
    if(NULL == psw_tmp)
    {
        return 0;
    }
    for(uw_i = 1u; uw_i <= pst_heap->uw_len; uw_i++)
    {
        psw_tmp[uw_i] = pst_heap->psw_aData[uw_i];
    }
    free(pst_heap->psw_aData);
    pst_heap->psw_aData = psw_tmp;
    pst_heap->uw_cap = uw_new_cap;
    return 1;
}

static void prvSwap(int *psw_a, int *psw_b)
{
    int sw_tmp = *psw_a;
    *psw_a = *psw_b;
    *psw_b = sw_tmp;
}

static void prvSiftUp(sHeap_t *pst_heap)
{
    unsigned int uw_i = pst_heap->uw_len;

    while(uw_i > 1u)
    {
        unsigned int uw_parent = uw_i / 2u;
        if(pst_heap->psw_aData[uw_i] <= pst_heap->psw_aData[uw_parent])
        {
            break;
        }
        prvSwap(&pst_heap->psw_aData[uw_i], &pst_heap->psw_aData[uw_parent]);
        uw_i = uw_parent;
    }
}

static void prvSiftDown(sHeap_t *pst_heap, unsigned int uw_start, unsigned int uw_end)
{
    unsigned int uw_i = uw_start;

    while(uw_i * 2u <= uw_end)
    {
        unsigned int uw_child = uw_i * 2u;
        unsigned int uw_max = uw_i;

        if(pst_heap->psw_aData[uw_child] > pst_heap->psw_aData[uw_max])
        {
            uw_max = uw_child;
        }
        if(uw_child + 1u <= uw_end &&
           pst_heap->psw_aData[uw_child + 1u] > pst_heap->psw_aData[uw_max])
        {
            uw_max = uw_child + 1u;
        }
        if(uw_max == uw_i)
        {
            break;
        }
        prvSwap(&pst_heap->psw_aData[uw_i], &pst_heap->psw_aData[uw_max]);
        uw_i = uw_max;
    }
}
```

---

## 并查集 unionfind

**是什么**：并查集（Union-Find）用于管理「不相交集合」。每个集合是一棵树，`parent[x]` 指向 x 的父结点，根即集合代表。`find` 找根，`union` 合并两树。为提速，本实现用**路径压缩**（find 时把沿途结点直接连到根）与**按秩合并**（小树挂到大树），使单次操作近乎常数。

**核心操作**：`unionfind_init`/`unionfind_destroy`/`unionfind_find`/`unionfind_union`/`unionfind_is_connected`/`unionfind_get_cnt`。元素 id 是整数，范围 [0, `uw_cnt`)。

**结构体字段**：父指针数组 `psw_parent`、秩数组 `puc_rank`、元素数 `uw_cnt`。

**典型用途**：网络连通性、Kruskal 最小生成树判环等。

### unionfind.h

```c
#ifndef UNIONFIND_H
#define UNIONFIND_H

#include "ds_types.h"

/* Union-find (disjoint set) with path compression and union by rank.
 * Elements are integers in [0, n). Each element maps to a parent index. */

typedef struct union_find
{
    int *psw_parent;     /* parent index of each element */
    unsigned char *puc_rank;    /* union-by-rank weight */
    unsigned int uw_cnt; /* number of elements */
} sUnion_find_t;

void         unionfind_init(sUnion_find_t *pst_uf, u32 u32Cnt);
void         unionfind_destroy(sUnion_find_t *pst_uf);
int          unionfind_find(sUnion_find_t *pst_uf, u32 u32Elem);
void         unionfind_union(sUnion_find_t *pst_uf, u32 u32A, u32 u32B);
int          unionfind_is_connected(sUnion_find_t *pst_uf, u32 u32A, u32 u32B);
unsigned int unionfind_get_cnt(const sUnion_find_t *pst_uf);

#endif /* UNIONFIND_H */
```

### unionfind.c

```c
/* Union-find (disjoint-set) with path compression on find and union by rank.
 * Elements are integer indices in [0, uw_cnt). */

/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: unionfind.c
Author: name
version: V1.0.0
function: Union-find - init, find, union, connectivity, count, destroy.
*/
/*
Notes:
1. Element ids are integer indices in [0, uw_cnt).
2. find applies path compression; union attaches the smaller rank tree.
*/
/*
Changelog:
1. Initial release.
*/

#include "unionfind.h"
#include <stdlib.h>

/* Forward declarations: private helpers first, then public functions. */
static int prvFindRoot(sUnion_find_t *pst_uf, unsigned int uw_elem);

void unionfind_init(sUnion_find_t *pst_uf, u32 u32Cnt)
{
    unsigned int uw_cnt = (unsigned int)u32Cnt;
    unsigned int uw_i;

    pst_uf->psw_parent = (int *)malloc(sizeof(int) * uw_cnt);
    pst_uf->puc_rank = (unsigned char *)malloc(sizeof(unsigned char) * uw_cnt);
    pst_uf->uw_cnt = uw_cnt;
    for(uw_i = 0u; uw_i < uw_cnt; uw_i++)
    {
        pst_uf->psw_parent[uw_i] = (int)uw_i;
        pst_uf->puc_rank[uw_i] = 0u;
    }
}

void unionfind_destroy(sUnion_find_t *pst_uf)
{
    free(pst_uf->psw_parent);
    free(pst_uf->puc_rank);
    pst_uf->psw_parent = NULL;
    pst_uf->puc_rank = NULL;
    pst_uf->uw_cnt = 0u;
}

int unionfind_find(sUnion_find_t *pst_uf, u32 u32Elem)
{
    return prvFindRoot(pst_uf, (unsigned int)u32Elem);
}

void unionfind_union(sUnion_find_t *pst_uf, u32 u32A, u32 u32B)
{
    unsigned int uw_ra = (unsigned int)prvFindRoot(pst_uf, u32A);
    unsigned int uw_rb = (unsigned int)prvFindRoot(pst_uf, u32B);

    if(uw_ra == uw_rb)
    {
        return;
    }
    if(pst_uf->puc_rank[uw_ra] < pst_uf->puc_rank[uw_rb])
    {
        pst_uf->psw_parent[uw_ra] = (int)uw_rb;
    }
    else if(pst_uf->puc_rank[uw_ra] > pst_uf->puc_rank[uw_rb])
    {
        pst_uf->psw_parent[uw_rb] = (int)uw_ra;
    }
    else
    {
        pst_uf->psw_parent[uw_rb] = (int)uw_ra;
        pst_uf->puc_rank[uw_ra]++;
    }
}

int unionfind_is_connected(sUnion_find_t *pst_uf, u32 u32A, u32 u32B)
{
    unsigned int uw_ra = (unsigned int)prvFindRoot(pst_uf, u32A);
    unsigned int uw_rb = (unsigned int)prvFindRoot(pst_uf, u32B);

    if(uw_ra == uw_rb)
    {
        return 1;
    }
    return 0;
}

unsigned int unionfind_get_cnt(const sUnion_find_t *pst_uf)
{
    return pst_uf->uw_cnt;
}

/* Private: return the root index of uw_elem, compressing the path. */
static int prvFindRoot(sUnion_find_t *pst_uf, unsigned int uw_elem)
{
    unsigned int uw_root = uw_elem;

    while((unsigned int)pst_uf->psw_parent[uw_root] != uw_root)
    {
        uw_root = (unsigned int)pst_uf->psw_parent[uw_root];
    }
    while((unsigned int)pst_uf->psw_parent[uw_elem] != uw_root)
    {
        unsigned int uw_next = (unsigned int)pst_uf->psw_parent[uw_elem];
        pst_uf->psw_parent[uw_elem] = (int)uw_root;
        uw_elem = uw_next;
    }
    return (int)uw_root;
}
```

---

## 排序算法 sort

**是什么**：7 种原地升序排序，统一操作一个 `s32` 数组：

| 算法 | 思想 | 最好 | 平均 | 最坏 | 稳定性 |
|------|------|------|------|------|--------|
| 插入 sort_insertion | 逐元素插入已有序区 | O(n) | O(n²) | O(n²) | 稳定 |
| 希尔 sort_shell | 分组插入，间距递减 | O(n log²n) | — | O(n²) | 不稳定 |
| 冒泡 sort_bubble | 相邻逆序交换 | O(n) | O(n²) | O(n²) | 稳定 |
| 快排 sort_quick | 分治按枢轴切分 | O(n log n) | O(n log n) | O(n²) | 不稳定 |
| 选择 sort_selection | 每趟选最小 | O(n²) | O(n²) | O(n²) | 不稳定 |
| 堆排 sort_heap | max-heap 反复取顶 | O(n log n) | O(n log n) | O(n log n) | 不稳定 |
| 归并 sort_merge | 分治合并有序段 | O(n log n) | O(n log n) | O(n log n) | 稳定 |

**特点**：快排用显式栈避免递归爆栈；归并需要 O(n) 辅助数组，但稳定。默认升序、原地排序。

### sort.h

```c
#ifndef SORT_H
#define SORT_H

#include "ds_types.h"

/* Sorting algorithms operating in place on an integer array.
 * All of them sort ascending; the source array is modified in place,
 * and the buffer argument gives the element count. */

void sort_insertion(s32 *ps32A, u32 u32Len);
void sort_shell(s32 *ps32A, u32 u32Len);
void sort_bubble(s32 *ps32A, u32 u32Len);
void sort_quick(s32 *ps32A, u32 u32Len);
void sort_selection(s32 *ps32A, u32 u32Len);
void sort_heap(s32 *ps32A, u32 u32Len);
void sort_merge(s32 *ps32A, u32 u32Len);

#endif /* SORT_H */
```

### sort.c

```c
/* In-place ascending sort algorithms over an s32 array.
 * Each entry point accepts the array and its length. */
/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: sort.c
Author: name
version: V1.0.0
function: Sorting - insertion, shell, bubble, quick, selection, heap, merge.
*/
/*
Notes:
1. All sorts are ascending and in place.
2. The array holds int values; swap is done on elements directly.
3. Quick sort uses an explicit stack (range based); merge sort is recursive.
*/
/*
Changelog:
1. Initial release.
*/

#include "sort.h"
#include <stdlib.h>
#include <string.h>

/* Forward declarations: private helpers first, then public functions. */
static void prvSwap(s32 *ps32A, s32 *ps32B);
static void prvMerge(s32 *ps32A, unsigned int uwLo, unsigned int uwMid, unsigned int uwHi);
static void prvMergeSortRec(s32 *ps32A, unsigned int uwLo, unsigned int uwHi);
static void prvSiftDown(s32 *ps32A, unsigned int uwStart, unsigned int uwEnd);

void sort_insertion(s32 *ps32A, u32 u32Len)
{
    unsigned int uw_i;
    unsigned int uw_j;
    int sw_key;

    for(uw_i = 1u; uw_i < (unsigned int)u32Len; uw_i++)
    {
        sw_key = (int)ps32A[uw_i];
        uw_j = uw_i;
        while(uw_j > 0u && (int)ps32A[uw_j - 1u] > sw_key)
        {
            ps32A[uw_j] = ps32A[uw_j - 1u];
            uw_j--;
        }
        ps32A[uw_j] = (s32)sw_key;
    }
}

void sort_shell(s32 *ps32A, u32 u32Len)
{
    unsigned int uw_gap = (unsigned int)u32Len / 2u;
    unsigned int uw_i;
    unsigned int uw_j;
    int sw_key;

    while(uw_gap > 0u)
    {
        for(uw_i = uw_gap; uw_i < (unsigned int)u32Len; uw_i++)
        {
            sw_key = (int)ps32A[uw_i];
            uw_j = uw_i;
            while(uw_j >= uw_gap && (int)ps32A[uw_j - uw_gap] > sw_key)
            {
                ps32A[uw_j] = ps32A[uw_j - uw_gap];
                uw_j -= uw_gap;
            }
            ps32A[uw_j] = (s32)sw_key;
        }
        uw_gap /= 2u;
    }
}

void sort_bubble(s32 *ps32A, u32 u32Len)
{
    unsigned int uw_i;
    unsigned int uw_j;
    unsigned int uw_n = (unsigned int)u32Len;

    for(uw_i = 0u; uw_i < uw_n - 1u; uw_i++)
    {
        for(uw_j = 0u; uw_j < uw_n - 1u - uw_i; uw_j++)
        {
            if((int)ps32A[uw_j] > (int)ps32A[uw_j + 1u])
            {
                prvSwap(&ps32A[uw_j], &ps32A[uw_j + 1u]);
            }
        }
    }
}

void sort_quick(s32 *ps32A, u32 u32Len)
{
    unsigned int uw_stackLo[64];
    unsigned int uw_stackHi[64];
    unsigned int uw_top;
    unsigned int uw_lo;
    unsigned int uw_hi;
    unsigned int uw_i;
    unsigned int uw_j;
    int sw_pivot;
    int sw_tmp;

    if((unsigned int)u32Len < 2u)
    {
        return;
    }
    uw_top = 0u;
    uw_stackLo[uw_top] = 0u;
    uw_stackHi[uw_top] = (unsigned int)u32Len - 1u;
    uw_top++;

    while(uw_top > 0u)
    {
        uw_top--;
        uw_lo = uw_stackLo[uw_top];
        uw_hi = uw_stackHi[uw_top];
        if(uw_lo >= uw_hi)
        {
            continue;
        }
        sw_pivot = (int)ps32A[(uw_lo + uw_hi) / 2u];
        uw_i = uw_lo;
        uw_j = uw_hi;
        while(uw_i <= uw_j)
        {
            while((int)ps32A[uw_i] < sw_pivot)
            {
                uw_i++;
            }
            while((int)ps32A[uw_j] > sw_pivot)
            {
                uw_j--;
            }
            if(uw_i <= uw_j)
            {
                sw_tmp = (int)ps32A[uw_i];
                ps32A[uw_i] = ps32A[uw_j];
                ps32A[uw_j] = (s32)sw_tmp;
                uw_i++;
                uw_j--;
            }
        }
        if(uw_lo < uw_j)
        {
            uw_stackLo[uw_top] = uw_lo;
            uw_stackHi[uw_top] = uw_j;
            uw_top++;
        }
        if(uw_i < uw_hi)
        {
            uw_stackLo[uw_top] = uw_i;
            uw_stackHi[uw_top] = uw_hi;
            uw_top++;
        }
    }
}

void sort_selection(s32 *ps32A, u32 u32Len)
{
    unsigned int uw_i;
    unsigned int uw_j;
    unsigned int uw_min;
    unsigned int uw_n = (unsigned int)u32Len;

    for(uw_i = 0u; uw_i < uw_n - 1u; uw_i++)
    {
        uw_min = uw_i;
        for(uw_j = uw_i + 1u; uw_j < uw_n; uw_j++)
        {
            if((int)ps32A[uw_j] < (int)ps32A[uw_min])
            {
                uw_min = uw_j;
            }
        }
        if(uw_min != uw_i)
        {
            prvSwap(&ps32A[uw_i], &ps32A[uw_min]);
        }
    }
}

void sort_heap(s32 *ps32A, u32 u32Len)
{
    unsigned int uw_i;
    unsigned int uw_n = (unsigned int)u32Len;

    if(uw_n < 2u)
    {
        return;
    }
    /* Build a max-heap over the 0-based array. */
    for(uw_i = uw_n / 2u; uw_i > 0u; uw_i--)
    {
        prvSiftDown(ps32A, uw_i - 1u, uw_n);
    }
    for(uw_i = uw_n; uw_i > 1u; uw_i--)
    {
        prvSwap(&ps32A[0], &ps32A[uw_i - 1u]);
        prvSiftDown(ps32A, 0u, uw_i - 1u);
    }
}

void sort_merge(s32 *ps32A, u32 u32Len)
{
    if((unsigned int)u32Len < 2u)
    {
        return;
    }
    prvMergeSortRec(ps32A, 0u, (unsigned int)u32Len - 1u);
}

static void prvSwap(s32 *ps32A, s32 *ps32B)
{
    s32 s32Tmp = *ps32A;
    *ps32A = *ps32B;
    *ps32B = s32Tmp;
}

static void prvMerge(s32 *ps32A, unsigned int uwLo, unsigned int uwMid, unsigned int uwHi)
{
    unsigned int uwLeft = uwLo;
    unsigned int uwRight = uwMid + 1u;
    unsigned int uwK = 0u;
    unsigned int uwTotal = uwHi - uwLo + 1u;
    s32 *ps32Tmp;

    ps32Tmp = (s32 *)malloc(sizeof(s32) * uwTotal);
    if(NULL == ps32Tmp)
    {
        return;
    }
    while(uwLeft <= uwMid && uwRight <= uwHi)
    {
        if((int)ps32A[uwLeft] <= (int)ps32A[uwRight])
        {
            ps32Tmp[uwK++] = ps32A[uwLeft++];
        }
        else
        {
            ps32Tmp[uwK++] = ps32A[uwRight++];
        }
    }
    while(uwLeft <= uwMid)
    {
        ps32Tmp[uwK++] = ps32A[uwLeft++];
    }
    while(uwRight <= uwHi)
    {
        ps32Tmp[uwK++] = ps32A[uwRight++];
    }
    for(uwK = 0u; uwK < uwTotal; uwK++)
    {
        ps32A[uwLo + uwK] = ps32Tmp[uwK];
    }
    free(ps32Tmp);
}

static void prvMergeSortRec(s32 *ps32A, unsigned int uwLo, unsigned int uwHi)
{
    unsigned int uwMid;

    if(uwLo >= uwHi)
    {
        return;
    }
    uwMid = (uwLo + uwHi) / 2u;
    prvMergeSortRec(ps32A, uwLo, uwMid);
    prvMergeSortRec(ps32A, uwMid + 1u, uwHi);
    prvMerge(ps32A, uwLo, uwMid, uwHi);
}

static void prvSiftDown(s32 *ps32A, unsigned int uwStart, unsigned int uwEnd)
{
    unsigned int uwI = uwStart;

    while(uwI * 2u + 1u < uwEnd)
    {
        unsigned int uwChild = uwI * 2u + 1u;
        unsigned int uwMax = uwI;

        if((int)ps32A[uwChild] > (int)ps32A[uwMax])
        {
            uwMax = uwChild;
        }
        if(uwChild + 1u < uwEnd && (int)ps32A[uwChild + 1u] > (int)ps32A[uwMax])
        {
            uwMax = uwChild + 1u;
        }
        if(uwMax == uwI)
        {
            break;
        }
        prvSwap(&ps32A[uwI], &ps32A[uwMax]);
        uwI = uwMax;
    }
}
```

---

## 查找算法 search

**是什么**：三种查找：

| 算法 | 前提 | 复杂度 | 返回 |
|------|------|--------|------|
| 顺序 search_sequential | 任意 | O(n) | 首次下标，未找到 -1 |
| 二分 search_binary | **已升序** | O(log n) | 下标，未找到 -1 |
| 排序辅助 search_sort_int | — | O(n²) | 供二分前排序 |

**特点**：二分查找必须在有序数组上进行（否则结果无意义）；`search_sort_int` 借用插入排序对未排序数据预排序，便于演示。

**教材补充**：教材的「树结构查找」与二叉排序树 `bst_find` 重合，故此处仅给出顺序表查找与折半查找的数组版本。

### search.h

```c
#ifndef SEARCH_H
#define SEARCH_H

#include "ds_types.h"

/* Search algorithms over an s32 array. The sequential and binary searches
 * operate on a flat array; binary search requires a sorted array. */

int  search_sequential(s32 *ps32A, u32 u32Len, s32 s32Key);
int  search_binary(s32 *ps32A, u32 u32Len, s32 s32Key);
void search_sort_int(s32 *ps32A, u32 u32Len);

#endif /* SEARCH_H */
```

### search.c

```c
/* Search algorithms over an s32 array.
 * search_sequential scans linearly; search_binary halves a sorted range. */
/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: search.c
Author: name
version: V1.0.0
function: Search - sequential, binary, and a helper sort for demo use.
*/
/*
Notes:
1. search_binary requires the array to be already sorted ascending.
2. search_sort_int wraps insertion sort so a demo can sort before searching.
*/
/*
Changelog:
1. Initial release.
*/

#include "search.h"
#include "sort.h"

int search_sequential(s32 *ps32A, u32 u32Len, s32 s32Key)
{
    unsigned int uw_i;
    unsigned int uw_n = (unsigned int)u32Len;

    for(uw_i = 0u; uw_i < uw_n; uw_i++)
    {
        if((int)ps32A[uw_i] == (int)s32Key)
        {
            return (int)uw_i;
        }
    }
    return -1;
}

int search_binary(s32 *ps32A, u32 u32Len, s32 s32Key)
{
    unsigned int uwLo = 0u;
    unsigned int uwHi = (unsigned int)u32Len - 1u;
    unsigned int uwMid;

    if(0u == (unsigned int)u32Len)
    {
        return -1;
    }
    while(uwLo <= uwHi)
    {
        uwMid = (uwLo + uwHi) / 2u;
        if((int)ps32A[uwMid] == (int)s32Key)
        {
            return (int)uwMid;
        }
        if((int)ps32A[uwMid] < (int)s32Key)
        {
            uwLo = uwMid + 1u;
        }
        else
        {
            if(0u == uwMid)
            {
                break;
            }
            uwHi = uwMid - 1u;
        }
    }
    return -1;
}

/* Helper: ascending insertion sort so a demo can prepare its data. */
void search_sort_int(s32 *ps32A, u32 u32Len)
{
    sort_insertion(ps32A, u32Len);
}
```

---

## 图 graph

**是什么**：图由顶点和边组成。本实现用**邻接矩阵**存储无向图：`auc_adj[u][v] == 1` 表示 u、v 之间有边（无向图因此是对称矩阵）。顶点 id 为整数 [0, `uw_num`)。

**核心操作**：`graph_init`/`graph_add_edge`/`graph_remove_edge`/`graph_has_edge`/`graph_dfs`（深度优先，递归）/`graph_bfs`（广度优先，环形队列）。DFS 沿一条分支走到底再回溯；BFS 按层扩展，借助队列。

**结构体字段**：邻接矩阵 `auc_adj[GRAPH_MAX_NODE][GRAPH_MAX_NODE]`、顶点数 `uw_num`，其中 `GRAPH_MAX_NODE` 为最大顶点数（16）。

### graph.h

```c
#ifndef GRAPH_H
#define GRAPH_H

#include "ds_types.h"

/* Graph stored as an adjacency matrix over integer vertex ids [0, n).
 * DFS and BFS are provided; BFS uses an internal ring queue. */

#define GRAPH_MAX_NODE 16u

typedef struct graph
{
    unsigned char auc_adj[GRAPH_MAX_NODE][GRAPH_MAX_NODE];   /* 1 = edge */
    unsigned int uw_num;      /* number of vertices */
} sGraph_t;

void  graph_init(sGraph_t *pst_graph, u32 u32Num);
void  graph_add_edge(sGraph_t *pst_graph, u32 u32U, u32 u32V);
void  graph_remove_edge(sGraph_t *pst_graph, u32 u32U, u32 u32V);
int   graph_has_edge(const sGraph_t *pst_graph, u32 u32U, u32 u32V);
void  graph_dfs(const sGraph_t *pst_graph, u32 u32Start);
void  graph_bfs(const sGraph_t *pst_graph, u32 u32Start);

#endif /* GRAPH_H */
```

### graph.c

```c
/* Graph on an adjacency matrix. DFS is recursive; BFS uses a ring queue.
 * Vertices are integer ids in [0, uw_num). */
/*
Copyright(c) 2025 ORG. All rights reserved.
fileName: graph.c
Author: name
version: V1.0.0
function: Graph - init, add/remove edge, has edge, DFS, BFS.
*/
/*
Notes:
1. Adjacency matrix is unsigned char; 1 means an edge exists.
2. DFS is recursive, BFS uses an internal ring queue of size GRAPH_MAX_NODE.
*/
/*
Changelog:
1. Initial release.
*/

#include "graph.h"
#include <stdio.h>

/* Forward declarations: private helpers first, then public functions. */
static void prvDfs(const sGraph_t *pst_graph, u32 u32V, unsigned char *pucVisited);

void graph_init(sGraph_t *pst_graph, u32 u32Num)
{
    unsigned int uw_i;
    unsigned int uw_j;

    pst_graph->uw_num = (unsigned int)u32Num;
    for(uw_i = 0u; uw_i < pst_graph->uw_num; uw_i++)
    {
        for(uw_j = 0u; uw_j < pst_graph->uw_num; uw_j++)
        {
            pst_graph->auc_adj[uw_i][uw_j] = 0u;
        }
    }
}

void graph_add_edge(sGraph_t *pst_graph, u32 u32U, u32 u32V)
{
    unsigned int uwU = (unsigned int)u32U;
    unsigned int uwV = (unsigned int)u32V;

    if(uwU >= pst_graph->uw_num || uwV >= pst_graph->uw_num)
    {
        return;
    }
    pst_graph->auc_adj[uwU][uwV] = 1u;
    pst_graph->auc_adj[uwV][uwU] = 1u;    /* undirected */
}

void graph_remove_edge(sGraph_t *pst_graph, u32 u32U, u32 u32V)
{
    unsigned int uwU = (unsigned int)u32U;
    unsigned int uwV = (unsigned int)u32V;

    if(uwU >= pst_graph->uw_num || uwV >= pst_graph->uw_num)
    {
        return;
    }
    pst_graph->auc_adj[uwU][uwV] = 0u;
    pst_graph->auc_adj[uwV][uwU] = 0u;
}

int graph_has_edge(const sGraph_t *pst_graph, u32 u32U, u32 u32V)
{
    unsigned int uwU = (unsigned int)u32U;
    unsigned int uwV = (unsigned int)u32V;

    if(uwU >= pst_graph->uw_num || uwV >= pst_graph->uw_num)
    {
        return 0;
    }
    if(1u == pst_graph->auc_adj[uwU][uwV])
    {
        return 1;
    }
    return 0;
}

/* Depth-first search from u32Start, printing vertex ids in visit order. */
void graph_dfs(const sGraph_t *pst_graph, u32 u32Start)
{
    unsigned char aucVisited[GRAPH_MAX_NODE] = {0u};
    unsigned int uw_i;

    for(uw_i = 0u; uw_i < pst_graph->uw_num; uw_i++)
    {
        aucVisited[uw_i] = 0u;
    }
    prvDfs(pst_graph, (unsigned int)u32Start, aucVisited);
    printf("\n");
}

/* Breadth-first search from u32Start, printing vertex ids in visit order. */
void graph_bfs(const sGraph_t *pst_graph, u32 u32Start)
{
    unsigned int aucQueue[GRAPH_MAX_NODE];
    unsigned char aucVisited[GRAPH_MAX_NODE] = {0u};
    unsigned int uwHead = 0u;
    unsigned int uwTail = 0u;
    unsigned int uwCur;
    unsigned int uwU;
    unsigned int uw_i;

    if(pst_graph->uw_num > GRAPH_MAX_NODE || (unsigned int)u32Start >= pst_graph->uw_num)
    {
        return;
    }
    for(uw_i = 0u; uw_i < pst_graph->uw_num; uw_i++)
    {
        aucVisited[uw_i] = 0u;
    }

    aucQueue[uwTail++] = (unsigned int)u32Start;
    aucVisited[(unsigned int)u32Start] = 1u;

    while(uwHead < uwTail)
    {
        uwCur = aucQueue[uwHead++];
        printf("%d ", (int)uwCur);
        for(uwU = 0u; uwU < pst_graph->uw_num; uwU++)
        {
            if(1u == pst_graph->auc_adj[uwCur][uwU] && 0u == aucVisited[uwU])
            {
                aucVisited[uwU] = 1u;
                aucQueue[uwTail++] = uwU;
            }
        }
    }
    printf("\n");
}

static void prvDfs(const sGraph_t *pst_graph, u32 u32V, unsigned char *pucVisited)
{
    unsigned int uwV = (unsigned int)u32V;
    unsigned int uwU;

    pucVisited[uwV] = 1u;
    printf("%d ", (int)uwV);
    for(uwU = 0u; uwU < pst_graph->uw_num; uwU++)
    {
        if(1u == pst_graph->auc_adj[uwV][uwU] && 0u == pucVisited[uwU])
        {
            prvDfs(pst_graph, (unsigned int)uwU, pucVisited);
        }
    }
}
```
---

## 邻接表法（笔记说明）

教材原文：**C 语言下只要求掌握图的 DFS 与 BFS 算法代码；本章图的结构全部使用 C++ 代码实现。** 邻接表法用「数组 `head[]` 存各顶点的出边链表」表示图，存储更省空间但实现依赖链表与动态内存。按「仅保留 C 版、且 DFS/BFS 已用邻接矩阵给出」的要求，此处只作说明不加代码；若确需 C 版邻接表 + 遍历，可仿照本册 `slist` 建立顶点结构体数组实现。

---

## 使用示例

以下用一个顺序表 + 二叉排序树的小例子演示如何调用（对应各 `.h` 声明）：

```c
#include "seq_list.h"
#include "bst.h"
#include <stdio.h>

int main(void)
{
    /* 顺序表 */
    sSeq_list_t st_list;
    seqlist_init(&st_list, 4u);
    seqlist_insert(&st_list, 0u, 10);
    seqlist_insert(&st_list, 1u, 20);
    seqlist_insert(&st_list, 1u, 15);
    seqlist_print(&st_list);
    seqlist_destroy(&st_list);

    /* 二叉排序树 */
    sBst_t st_bst;
    bst_init(&st_bst);
    bst_insert(&st_bst, 50);
    bst_insert(&st_bst, 30);
    bst_insert(&st_bst, 70);
    printf("in-order: ");
    bst_inorder(&st_bst);
    bst_destroy(&st_bst);
    return 0;
}
```

> 每个模块都是「`sXxx_t` 结构体 + `xxx_*` 函数」的独立单元；`.h` 文件头已省略注释块（HDR-05），`.c` 文件头含版权/版本说明。
