---
title: 终身个人知识库（Obsidian + 卡片盒 + Git 云端）
tags: [知识库, obsidian, zettelkasten, git]
---

# 终身个人知识库（Obsidian + 卡片盒 + Git 云端）

> 多领域 + 卡片盒 + 终身云端，换电脑 git clone 即恢复。

## 核心原则
1. **纯 Markdown + 相对路径** —— 不锁死 Obsidian，任何工具都能读
2. **图片/附件全部进 vault 内部** —— 用相对路径引用，整体搬走/换机不失效
3. **git 仓库可完整重建** —— 连设置、模板一起提交，换电脑 clone 即恢复

## 目录结构
```
obsidian_test/                 ← vault 根 = git 仓库根
├── .obsidian/                 ← Obsidian 配置（部分提交，见 gitignore）
├── .gitignore
├── README.md                  ← 库说明：怎么用、怎么同步
├── 首页.md                    ← 全局总入口，链到各领域
├── Inbox/                     ← 跨领域闪念收件箱
│   └── 0-说明.md
├── 技术/
│   ├── _inbox/                ← 领域闪念
│   ├── _cards/                ← 永久卡片（时间戳 ID）
│   ├── _literature/           ← 文献笔记
│   ├── _assets/               ← 该领域图片/附件
│   └── _moc.md                ← 领域索引
├── 工作/  生活/  学习/        ← 同上结构
├── 记录/                      ← 记录区（带时间轴，跨领域）
│   ├── 日志/                  ← 日记，Daily Notes，YYYY-MM-DD
│   ├── 周报/                  ← 每周复盘
│   ├── 收支/                  ← 财务流水
│   ├── 单词/                  ← 记单词
│   └── _assets/               ← 记录区附件
├── _assets/                   ← 全局共用附件（默认附件目录）
└── _templates/
    ├── 卡片.md
    ├── 文献笔记.md
    └── MOC.md
```

用 `_` 前缀把「结构性文件夹」和「内容」在文件列表里分开，结构目录自动沉底。

## 图片方案（重点）
- Obsidian「设置 → 文件与链接 → 新附件的默认位置」设为 `_assets/`（相对路径）
- 配置写进 `.obsidian/app.json` 并提交，换电脑设置自动生效
- 引用用 `![[图片名.png]]`（Obsidian 内部）或 `![描述](../_assets/图片名.png)`（通用 Markdown）
- 所有图都在 vault 内，git 一并同步，绝对路径不出现

## 双向链接方案
- 卡片间 `[[另一张卡片]]` 互联 → 反向链接面板 + 关系图谱自动可视化
- 每个领域 `_moc.md` 做人工索引；`首页.md` 链接各领域 MOC
- 卡片用时间戳 ID 命名（如 `202608141130.md`），标题是人类可读主题名，链接用 `[[标题]]` 而非 ID

## 卡片命名与 ID
- 卡片文件用时间戳 ID 做文件名（`202608141130.md`，年月日时分）
- 标题（`#` 首行）用人类可读的主题名，ID 只当文件名，避免重名、改名破坏链接
- 闪念文件用标题命名（`想学 Rust.md`），整理后再转成 ID 卡片

## 标签体系
- `#领域/子主题`，如 `#技术/rust`、`#工作/项目A`
- 状态标签 `#状态/闪念`、`#状态/永久` 区分成熟度
- 记录区用 `#记录/日志`、`#记录/周报`、`#记录/收支`、`#记录/单词`

## 记录区（Journal）
带时间轴、跨领域的记录型笔记，独立成一个「记录区」，不套卡片盒 ID 命名（按时序翻查）。

| 类型 | 文件命名 | 机制 |
|---|---|---|
| 日志 | `2026-08-14.md` | Obsidian Daily Notes 核心插件自动建，默认目录=记录/日志、默认模板=日志模板 |
| 周报 | `2026-W33.md` | 模板手动建（或每周末建一张） |
| 收支 | `2026-08-14-流水.md` | 模板建，表格：日期/类别/金额/备注 |
| 单词 | `单词本.md`（累计）或按天 | 模板建，表格：单词/音标/释义/例句/掌握度 |

## 模板（共 7 个）
- 卡片.md、文献笔记.md、MOC.md（3 个基础）
- 日志.md（# 日期 / 今日完成 / 想法 / 明日计划）
- 周报.md（本周回顾 / 收获 / 问题 / 下周计划）
- 收支.md（流水表格 + 月度汇总）
- 单词.md（单词表格 + 复习标记）

## 插件配置
- 默认不装第三方插件（YAGNI），只配 Core 插件：模板（指向 `_templates/`）、标签面板、关系图谱、反向链接
- 提供 README.md 说明怎么加模板/新增领域目录

## 云端 + 换电脑流程
1. obsidian_test 内 `git init` → 提交 → push 到 GitHub
2. 提交内容：所有笔记、模板、附件、`.obsidian/` 里的核心配置（app.json、核心插件开关、模板设置）
3. `.gitignore` 排除：`.obsidian/workspace*.json`（本机窗口状态）、`.trash/`、`.DS_Store`
4. 换电脑：装 Obsidian → `git clone <repo>` → 用 Obsidian 打开该文件夹 → 全部恢复（图片、模板、双向链接都在）

## 交付清单
1. 目录骨架（4 领域 ×4 子目录 + Inbox + 记录区(4 子目录) + 全局/记录区 _assets + _templates）
2. 模板 ×7（卡片/文献笔记/MOC/日志/周报/收支/单词）
3. 4 领域 MOC + 首页 + Inbox 说明 + 记录区说明
4. 示例卡片（演示 ID 命名 + 双向链接 + 图片引用）
5. `.obsidian/app.json`（相对路径附件 + Daily Notes 配置）+ `.gitignore`
6. README.md（使用手册 + 同步手册）

## 落地实例
- 仓库：`a2592979759/obsidian-knowledge-base`（公开，Steven long）
- 推送成功步骤：建仓库 → `git remote add origin https://github.com/a2592979759/obsidian-knowledge-base.git` → `git push -u origin master` → 本地 master 跟踪 origin/master
- GitHub token 用 `gho_` OAuth token（经 credential manager 获取，不打印）
